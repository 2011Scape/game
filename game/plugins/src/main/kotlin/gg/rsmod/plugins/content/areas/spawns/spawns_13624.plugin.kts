package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GULL_9717,
    x = 3392,
    z = 3603,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gull
spawn_npc(
    npc = Npcs.GULL_9717,
    x = 3392,
    z = 3638,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gull
spawn_npc(
    npc = Npcs.GULL_9717,
    x = 3416,
    z = 3608,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gull
spawn_npc(
    npc = Npcs.GULL_9717,
    x = 3431,
    z = 3624,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gull
spawn_npc(
    npc = Npcs.LOBSTER,
    x = 3403,
    z = 3614,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lobster
spawn_npc(
    npc = Npcs.LOBSTER,
    x = 3411,
    z = 3625,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lobster
spawn_npc(
    npc = Npcs.LOBSTER,
    x = 3439,
    z = 3633,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lobster
spawn_npc(npc = Npcs.ROOK, x = 3396, z = 3614, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rook
spawn_npc(npc = Npcs.ROOK, x = 3406, z = 3631, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rook
spawn_npc(npc = Npcs.ROOK, x = 3417, z = 3644, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rook

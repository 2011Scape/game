package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2296,
    z = 3000,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad

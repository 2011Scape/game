package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BRIMSTAIL,
    x = 2410,
    z = 9819,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brimstail

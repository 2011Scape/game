package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.FORESTER,
    x = 2650,
    z = 3468,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Forester
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2625,
    z = 3502,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2629,
    z = 3475,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2631,
    z = 3472,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2636,
    z = 3503,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2645,
    z = 3488,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2659,
    z = 3478,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2665,
    z = 3471,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2666,
    z = 3506,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2669,
    z = 3488,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.GUARD_DOG,
    x = 2674,
    z = 3504,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard dog
spawn_npc(
    npc = Npcs.TRAMP_2793,
    x = 2683,
    z = 3484,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tramp

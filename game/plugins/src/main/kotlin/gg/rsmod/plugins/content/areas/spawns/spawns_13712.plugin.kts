package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.SCARAB_LARVA,
    x = 3395,
    z = 9229,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scarab larva
spawn_npc(
    npc = Npcs.SCARAB_LARVA,
    x = 3396,
    z = 9233,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scarab larva
spawn_npc(
    npc = Npcs.SCARAB_LARVA,
    x = 3401,
    z = 9232,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scarab larva
spawn_npc(
    npc = Npcs.SCARAB_LARVA,
    x = 3404,
    z = 9233,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scarab larva
spawn_npc(
    npc = Npcs.SCARAB_LARVA,
    x = 3405,
    z = 9225,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scarab larva
spawn_npc(
    npc = Npcs.SCARAB_LARVA,
    x = 3409,
    z = 9232,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scarab larva
spawn_npc(
    npc = Npcs.CLAY_GOLEM,
    x = 3442,
    z = 9245,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scarab larva
spawn_npc(
    npc = Npcs.GIANT_SCARAB_6771,
    x = 3421,
    z = 9263,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scarab larva

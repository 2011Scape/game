package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.LONGRAMBLE,
    x = 2340,
    z = 3096,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Longramble

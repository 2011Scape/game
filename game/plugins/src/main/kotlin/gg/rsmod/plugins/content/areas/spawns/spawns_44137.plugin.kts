package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.KURASK_OVERLORD,
    x = 11056,
    z = 6768,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kurask overlord

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.MUGGER,
    x = 3142,
    z = 4230,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mugger
spawn_npc(
    npc = Npcs.MUGGER,
    x = 3145,
    z = 4231,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mugger
spawn_npc(
    npc = Npcs.MUGGER,
    x = 3145,
    z = 4229,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mugger
spawn_npc(
    npc = Npcs.MUGGER,
    x = 3146,
    z = 4232,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mugger
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3149,
    z = 4235,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3144,
    z = 4236,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3143,
    z = 4236,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3144,
    z = 4240,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3142,
    z = 4241,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3142,
    z = 4244,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3141,
    z = 4249,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3145,
    z = 4249,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3148,
    z = 4252,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3148,
    z = 4256,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3148,
    z = 4260,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3148,
    z = 4264,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3148,
    z = 4268,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3151,
    z = 4270,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3155,
    z = 4270,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3157,
    z = 4268,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3157,
    z = 4266,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3157,
    z = 4262,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3160,
    z = 4260,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3164,
    z = 4254,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3165,
    z = 4250,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3166,
    z = 4248,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3164,
    z = 4246,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3160,
    z = 4245,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3158,
    z = 4244,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3159,
    z = 4241,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3162,
    z = 4238,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3163,
    z = 4236,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3162,
    z = 4234,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3162,
    z = 4232,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3163,
    z = 4230,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3167,
    z = 4229,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_DRONE,
    x = 3169,
    z = 4229,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach drone
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3175,
    z = 4237,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3175,
    z = 4244,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3176,
    z = 4253,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3175,
    z = 4267,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3173,
    z = 4266,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3173,
    z = 4262,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3173,
    z = 4258,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3173,
    z = 4252,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3169,
    z = 4266,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3165,
    z = 4271,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3159,
    z = 4271,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3151,
    z = 4271,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3143,
    z = 4271,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3139,
    z = 4269,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3142,
    z = 4265,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3145,
    z = 4264,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3145,
    z = 4258,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3143,
    z = 4254,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3139,
    z = 4248,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3139,
    z = 4241,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3145,
    z = 4239,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3145,
    z = 4231,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3147,
    z = 4227,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3157,
    z = 4225,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3159,
    z = 4235,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3160,
    z = 4241,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3167,
    z = 4244,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3167,
    z = 4250,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_WORKER,
    x = 3161,
    z = 4254,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach worker
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3161,
    z = 4249,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3161,
    z = 4267,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3155,
    z = 4269,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3160,
    z = 4275,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3173,
    z = 4268,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3174,
    z = 4252,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3174,
    z = 4239,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3188,
    z = 4235,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3190,
    z = 4231,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3187,
    z = 4254,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3192,
    z = 4247,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3170,
    z = 4267,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3158,
    z = 4276,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3151,
    z = 4275,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier
spawn_npc(
    npc = Npcs.COCKROACH_SOLDIER,
    x = 3151,
    z = 4279,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cockroach soldier

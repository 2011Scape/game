package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1875,
    z = 3207,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1878,
    z = 3210,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1880,
    z = 3206,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1882,
    z = 3208,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1886,
    z = 3207,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1891,
    z = 3208,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1891,
    z = 3251,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1892,
    z = 3254,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1896,
    z = 3250,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1897,
    z = 3253,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1903,
    z = 3250,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly
spawn_npc(
    npc = Npcs.JELLY_8599,
    x = 1903,
    z = 3254,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly

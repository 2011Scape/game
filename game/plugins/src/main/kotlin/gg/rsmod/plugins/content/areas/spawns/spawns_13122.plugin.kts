package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BASILISK,
    x = 3264,
    z = 4256,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Basilisk

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.HELLHOUND,
    x = 3085,
    z = 10134,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hellhound
spawn_npc(
    npc = Npcs.HELLHOUND,
    x = 3090,
    z = 10123,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hellhound
spawn_npc(
    npc = Npcs.HELLHOUND,
    x = 3094,
    z = 10133,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hellhound
spawn_npc(
    npc = Npcs.HELLHOUND,
    x = 3095,
    z = 10120,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hellhound
spawn_npc(
    npc = Npcs.HELLHOUND,
    x = 3095,
    z = 10128,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hellhound
spawn_npc(
    npc = Npcs.HELLHOUND,
    x = 3098,
    z = 10122,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hellhound
spawn_npc(
    npc = Npcs.HELLHOUND,
    x = 3104,
    z = 10127,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hellhound
spawn_npc(
    npc = Npcs.HELLHOUND,
    x = 3105,
    z = 10125,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hellhound
spawn_npc(
    npc = Npcs.REVENANT_IMP,
    x = 3120,
    z = 10135,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant imp
spawn_npc(
    npc = Npcs.REVENANT_IMP,
    x = 3088,
    z = 10162,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant imp
spawn_npc(
    npc = Npcs.REVENANT_GOBLIN,
    x = 3094,
    z = 10165,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant goblin
spawn_npc(
    npc = Npcs.REVENANT_GOBLIN_13468,
    x = 3121,
    z = 10144,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant goblin
spawn_npc(
    npc = Npcs.REVENANT_GOBLIN_13469,
    x = 3095,
    z = 10152,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant goblin
spawn_npc(
    npc = Npcs.REVENANT_ICEFIEND,
    x = 3097,
    z = 10156,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant icefiend
spawn_npc(
    npc = Npcs.REVENANT_PYREFIEND,
    x = 3127,
    z = 10145,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant pyrefiend
spawn_npc(
    npc = Npcs.REVENANT_HOBGOBLIN,
    x = 3101,
    z = 10169,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant hobgoblin
spawn_npc(
    npc = Npcs.REVENANT_VAMPYRE,
    x = 3107,
    z = 10164,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant vampyre
spawn_npc(
    npc = Npcs.REVENANT_WEREWOLF,
    x = 3094,
    z = 10168,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant werewolf
spawn_npc(
    npc = Npcs.REVENANT_HELLHOUND,
    x = 3103,
    z = 10167,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant hellhound
spawn_npc(
    npc = Npcs.REVENANT_DEMON,
    x = 3116,
    z = 10155,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant demon
spawn_npc(
    npc = Npcs.REVENANT_ORK,
    x = 3109,
    z = 10148,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant ork
spawn_npc(
    npc = Npcs.REVENANT_DARK_BEAST,
    x = 3117,
    z = 10145,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant dark beast
spawn_npc(
    npc = Npcs.REVENANT_KNIGHT,
    x = 3091,
    z = 10150,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant knight
spawn_npc(
    npc = Npcs.REVENANT_DRAGON,
    x = 3116,
    z = 10165,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Revenant dragon

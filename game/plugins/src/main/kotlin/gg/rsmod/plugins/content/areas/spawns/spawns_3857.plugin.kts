package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.SMUGGLER,
    x = 983,
    z = 1125,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Smuggler

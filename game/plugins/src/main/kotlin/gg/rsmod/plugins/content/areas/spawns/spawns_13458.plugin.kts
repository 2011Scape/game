package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CATOLAX,
    x = 3366,
    z = 9352,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee
spawn_npc(
    npc = Npcs.BANSHEE_MISTRESS,
    x = 3359,
    z = 9435,
    height = 0,
    walkRadius = 5,
    direction = Direction.SOUTH,
    static = false,
) // Mighty banshee

spawn_npc(
    npc = Npcs.MIGHTY_BANSHEE,
    x = 3352,
    z = 9422,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee
spawn_npc(
    npc = Npcs.MIGHTY_BANSHEE,
    x = 3365,
    z = 9417,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee
spawn_npc(
    npc = Npcs.MIGHTY_BANSHEE,
    x = 3350,
    z = 9410,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee
spawn_npc(
    npc = Npcs.MIGHTY_BANSHEE,
    x = 3365,
    z = 9403,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee
spawn_npc(
    npc = Npcs.MIGHTY_BANSHEE,
    x = 3367,
    z = 9390,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee
spawn_npc(
    npc = Npcs.MIGHTY_BANSHEE,
    x = 3366,
    z = 9378,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee
spawn_npc(
    npc = Npcs.MIGHTY_BANSHEE,
    x = 3352,
    z = 9377,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee
spawn_npc(
    npc = Npcs.MIGHTY_BANSHEE,
    x = 3351,
    z = 9386,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee
spawn_npc(
    npc = Npcs.MIGHTY_BANSHEE,
    x = 3351,
    z = 9402,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mighty banshee

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.PIRATE_PETE_2826,
    x = 2112,
    z = 5064,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate Pete
spawn_npc(
    npc = Npcs.PIRATE_PETE_2826,
    x = 2163,
    z = 5113,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate Pete
spawn_npc(
    npc = Npcs.CAPTAIN_BRAINDEATH,
    x = 2144,
    z = 5109,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Captain Braindeath
spawn_npc(
    npc = Npcs._50_LUKE,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // 50% Luke
spawn_npc(npc = Npcs.DAVEY, x = 2132, z = 5100, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Davey
spawn_npc(
    npc = Npcs.CAPTAIN_DONNIE,
    x = 2145,
    z = 5078,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Captain Donnie
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER,
    x = 2159,
    z = 5086,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2832,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2832,
    x = 2154,
    z = 5081,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2833,
    x = 2149,
    z = 5087,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2833,
    x = 2152,
    z = 5087,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2834,
    x = 2149,
    z = 5082,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2834,
    x = 2156,
    z = 5086,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2835,
    x = 2145,
    z = 5082,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2835,
    x = 2154,
    z = 5082,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2836,
    x = 2144,
    z = 5086,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2836,
    x = 2144,
    z = 5088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PROTESTER_2836,
    x = 2157,
    z = 5081,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie protester
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE,
    x = 2139,
    z = 5084,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2838,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2838,
    x = 2163,
    z = 5074,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2839,
    x = 2141,
    z = 5078,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2839,
    x = 2162,
    z = 5082,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2840,
    x = 2144,
    z = 5078,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2840,
    x = 2159,
    z = 5082,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2841,
    x = 2142,
    z = 5078,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2841,
    x = 2156,
    z = 5088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2842,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_2842,
    x = 2159,
    z = 5081,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_SWAB,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie swab
spawn_npc(
    npc = Npcs.ZOMBIE_SWAB_2844,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie swab
spawn_npc(
    npc = Npcs.ZOMBIE_SWAB_2845,
    x = 2152,
    z = 5072,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie swab
spawn_npc(
    npc = Npcs.ZOMBIE_SWAB_2846,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie swab
spawn_npc(
    npc = Npcs.ZOMBIE_SWAB_2847,
    x = 2147,
    z = 5071,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie swab
spawn_npc(
    npc = Npcs.ZOMBIE_SWAB_2848,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie swab
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2112,
    z = 5064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2145,
    z = 5100,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2147,
    z = 5101,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2151,
    z = 5094,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2155,
    z = 5103,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2159,
    z = 5099,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.FEVER_SPIDER,
    x = 2159,
    z = 5099,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fever spider
spawn_npc(
    npc = Npcs.BREWER,
    x = 2152,
    z = 5089,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brewer
spawn_npc(
    npc = Npcs.BREWER_2852,
    x = 2155,
    z = 5089,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brewer
spawn_npc(
    npc = Npcs.BREWER_2853,
    x = 2112,
    z = 5064,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brewer
spawn_npc(
    npc = Npcs.BREWER_2854,
    x = 2159,
    z = 5097,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brewer
spawn_npc(
    npc = Npcs.BREWER_2855,
    x = 2112,
    z = 5064,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brewer
spawn_npc(
    npc = Npcs.BREWER_2856,
    x = 2155,
    z = 5092,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brewer
spawn_npc(
    npc = Npcs.BREWER_2857,
    x = 2155,
    z = 5094,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brewer
spawn_npc(
    npc = Npcs.BREWER_2858,
    x = 2151,
    z = 5099,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brewer
spawn_npc(npc = Npcs.FISHING_SPOT_2859, x = 2134, z = 5060, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_2859, x = 2162, z = 5061, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_2859, x = 2173, z = 5074, height = 0, direction = Direction.NORTH, static = true) // Fishing spot

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.LANTHUS,
    x = 2442,
    z = 3089,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lanthus
spawn_npc(
    npc = Npcs.POSTIE_PETE,
    x = 2446,
    z = 3097,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Postie Pete

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2860,
    z = 9911,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_WARRIOR,
    x = 2819,
    z = 9903,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice warrior added by jazneta
spawn_npc(
    npc = Npcs.ICE_WARRIOR,
    x = 2832,
    z = 9901,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice warrior added by jazneta
spawn_npc(
    npc = Npcs.ICE_WARRIOR,
    x = 2839,
    z = 9919,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice warrior added by jazneta
spawn_npc(
    npc = Npcs.ICE_WARRIOR,
    x = 2847,
    z = 9912,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice warrior added by jazneta
spawn_npc(
    npc = Npcs.CAPTAIN_NINTO,
    x = 2865,
    z = 9874,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Captain Ninto
spawn_npc(npc = Npcs.ROHAK, x = 2867, z = 9877, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rohak added by devin

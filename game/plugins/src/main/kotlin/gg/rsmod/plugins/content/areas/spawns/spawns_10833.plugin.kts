package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CAVE_CRAWLER,
    x = 2713,
    z = 5217,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave crawler
spawn_npc(
    npc = Npcs.CAVE_CRAWLER_1601,
    x = 2717,
    z = 5223,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave crawler
spawn_npc(
    npc = Npcs.CAVE_CRAWLER_1601,
    x = 2713,
    z = 5214,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave crawler
spawn_npc(
    npc = Npcs.CAVE_CRAWLER_1602,
    x = 2713,
    z = 5201,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave crawler
spawn_npc(
    npc = Npcs.CAVE_CRAWLER_1603,
    x = 2707,
    z = 5217,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave crawler
spawn_npc(
    npc = Npcs.ROCKSLUG,
    x = 2728,
    z = 5221,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rockslug
spawn_npc(
    npc = Npcs.ROCKSLUG,
    x = 2730,
    z = 5221,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rockslug
spawn_npc(
    npc = Npcs.ROCKSLUG,
    x = 2728,
    z = 5223,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rockslug
spawn_npc(
    npc = Npcs.ROCKSLUG_1632,
    x = 2731,
    z = 5222,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rockslug
spawn_npc(
    npc = Npcs.GIANT_FROG,
    x = 2730,
    z = 5208,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant frog
spawn_npc(
    npc = Npcs.GIANT_FROG,
    x = 2745,
    z = 5206,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant frog
spawn_npc(
    npc = Npcs.GIANT_FROG,
    x = 2730,
    z = 5192,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2699,
    z = 5216,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2698,
    z = 5218,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2695,
    z = 5222,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2695,
    z = 5223,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2728,
    z = 5201,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2729,
    z = 5190,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2730,
    z = 5191,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2729,
    z = 5200,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2739,
    z = 5194,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2743,
    z = 5199,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2743,
    z = 5203,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2744,
    z = 5204,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.BIG_FROG,
    x = 2744,
    z = 5202,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Big frog
spawn_npc(
    npc = Npcs.CAVE_SLIME,
    x = 2732,
    z = 5234,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave slime
spawn_npc(
    npc = Npcs.CAVE_SLIME,
    x = 2727,
    z = 5236,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave slime
spawn_npc(
    npc = Npcs.CAVE_SLIME,
    x = 2727,
    z = 5237,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave slime
spawn_npc(
    npc = Npcs.CAVE_SLIME,
    x = 2700,
    z = 5227,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave slime
spawn_npc(
    npc = Npcs.CAVE_SLIME,
    x = 2700,
    z = 5235,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave slime
spawn_npc(
    npc = Npcs.CAVE_SLIME,
    x = 2700,
    z = 5234,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave slime
spawn_npc(
    npc = Npcs.CAVE_SLIME,
    x = 2699,
    z = 5236,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave slime
spawn_npc(
    npc = Npcs.CAVE_SLIME,
    x = 2697,
    z = 5234,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave slime
spawn_npc(
    npc = Npcs.CAVE_SLIME,
    x = 2698,
    z = 5237,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave slime
spawn_npc(
    npc = Npcs.CAVE_BUG,
    x = 2744,
    z = 5222,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG,
    x = 2738,
    z = 5208,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG,
    x = 2722,
    z = 5224,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG,
    x = 2720,
    z = 5235,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG,
    x = 2719,
    z = 5236,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG,
    x = 2714,
    z = 5234,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG,
    x = 2713,
    z = 5235,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG,
    x = 2710,
    z = 5230,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG,
    x = 2713,
    z = 5219,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2739,
    z = 5231,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2735,
    z = 5234,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2745,
    z = 5217,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2746,
    z = 5216,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2746,
    z = 5226,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2733,
    z = 5234,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2723,
    z = 5235,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2715,
    z = 5237,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2715,
    z = 5232,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2711,
    z = 5231,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2699,
    z = 5232,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2703,
    z = 5216,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2713,
    z = 5208,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2726,
    z = 5199,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2721,
    z = 5199,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2712,
    z = 5192,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2728,
    z = 5188,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(
    npc = Npcs.CAVE_BUG_LARVA,
    x = 2737,
    z = 5205,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug larva
spawn_npc(npc = Npcs.FISHING_SPOT_2068, x = 2747, z = 5227, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_2068, x = 2694, z = 5225, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(
    npc = Npcs.CAVE_BUG_5750,
    x = 2745,
    z = 5221,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG_5750,
    x = 2740,
    z = 5208,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG_5750,
    x = 2716,
    z = 5228,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG_5750,
    x = 2714,
    z = 5237,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG_5750,
    x = 2698,
    z = 5224,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.CAVE_BUG_5750,
    x = 2743,
    z = 5196,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cave bug
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2745,
    z = 5207,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2745,
    z = 5204,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2744,
    z = 5200,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2742,
    z = 5196,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2738,
    z = 5192,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2732,
    z = 5206,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2730,
    z = 5202,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2727,
    z = 5199,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2730,
    z = 5195,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2733,
    z = 5191,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2727,
    z = 5188,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2721,
    z = 5200,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2744,
    z = 5221,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2745,
    z = 5227,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2743,
    z = 5230,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2737,
    z = 5233,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2732,
    z = 5235,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2714,
    z = 5234,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2710,
    z = 5230,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2706,
    z = 5227,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2701,
    z = 5227,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2697,
    z = 5231,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2699,
    z = 5237,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(
    npc = Npcs.MOLANISK,
    x = 2740,
    z = 5208,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Molanisk
spawn_npc(npc = 6398, x = 2737, z = 5220, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Zanik
spawn_npc(npc = 6400, x = 2741, z = 5220, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Oldak
spawn_npc(npc = 8651, x = 2713, z = 5191, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Zanik

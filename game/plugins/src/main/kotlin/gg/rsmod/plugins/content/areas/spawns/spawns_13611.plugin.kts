package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.ASSISTANT_ARCHAEOLOGIST_KERNER,
    x = 3408,
    z = 2813,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Assistant archaeologist Kerner
spawn_npc(
    npc = Npcs.CROCODILE_6779,
    x = 3392,
    z = 2759,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE_6779,
    x = 3408,
    z = 2763,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE_6779,
    x = 3410,
    z = 2786,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE_6779,
    x = 3420,
    z = 2778,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE_6779,
    x = 3427,
    z = 2792,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE_6779,
    x = 3429,
    z = 2802,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.LEAD_ARCHAEOLOGIST_ABIGAIL,
    x = 3409,
    z = 2812,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lead archaeologist Abigail
spawn_npc(
    npc = Npcs.LOCUST_LANCER,
    x = 3432,
    z = 2810,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Locust lancer
spawn_npc(
    npc = Npcs.LOCUST_RANGER,
    x = 3440,
    z = 2806,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Locust ranger
spawn_npc(
    npc = Npcs.SCABARAS_LANCER,
    x = 3435,
    z = 2808,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras lancer
spawn_npc(
    npc = Npcs.SCABARAS_LOCUST,
    x = 3441,
    z = 2810,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras locust
spawn_npc(
    npc = Npcs.SCABARAS_LOCUST,
    x = 3445,
    z = 2813,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras locust
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3439,
    z = 2808,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger

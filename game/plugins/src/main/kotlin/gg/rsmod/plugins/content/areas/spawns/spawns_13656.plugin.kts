package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3436,
    z = 5668,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3439,
    z = 5665,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3437,
    z = 5662,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3433,
    z = 5662,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3432,
    z = 5659,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3434,
    z = 5656,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3429,
    z = 5656,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3432,
    z = 5652,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3429,
    z = 5651,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3424,
    z = 5654,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3419,
    z = 5654,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3415,
    z = 5654,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3414,
    z = 5659,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3417,
    z = 5660,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3415,
    z = 5664,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3417,
    z = 5668,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3417,
    z = 5672,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3414,
    z = 5673,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3413,
    z = 5677,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3412,
    z = 5674,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3422,
    z = 5677,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound
spawn_npc(
    npc = Npcs.MOUND_9462,
    x = 3425,
    z = 5673,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mound

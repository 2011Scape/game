package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.ZOMBIE_74,
    x = 3123,
    z = 9648,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie added by devin
spawn_npc(
    npc = Npcs.ZOMBIE_74,
    x = 3122,
    z = 9661,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie added by devin
spawn_npc(
    npc = Npcs.ZOMBIE_5304,
    x = 3119,
    z = 9646,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie added by devin
spawn_npc(
    npc = Npcs.ZOMBIE_5304,
    x = 3125,
    z = 9658,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie added by devin
spawn_npc(npc = Npcs.RAT, x = 3124, z = 9660, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat added by devin

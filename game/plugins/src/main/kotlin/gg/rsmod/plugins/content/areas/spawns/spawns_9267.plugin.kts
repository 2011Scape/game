package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.DEADLY_RED_SPIDER,
    x = 2338,
    z = 3310,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Deadly red spider
spawn_npc(
    npc = Npcs.DEADLY_RED_SPIDER,
    x = 2341,
    z = 3309,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Deadly red spider
spawn_npc(
    npc = Npcs.DEADLY_RED_SPIDER,
    x = 2343,
    z = 3309,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Deadly red spider
spawn_npc(
    npc = Npcs.DEADLY_RED_SPIDER,
    x = 2344,
    z = 3318,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Deadly red spider
spawn_npc(
    npc = Npcs.DEADLY_RED_SPIDER,
    x = 2345,
    z = 3310,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Deadly red spider
spawn_npc(
    npc = Npcs.DEADLY_RED_SPIDER,
    x = 2345,
    z = 3312,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Deadly red spider
spawn_npc(
    npc = Npcs.DEADLY_RED_SPIDER,
    x = 2350,
    z = 3310,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Deadly red spider
spawn_npc(
    npc = Npcs.DIRE_WOLF,
    x = 2311,
    z = 3271,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dire Wolf
spawn_npc(
    npc = Npcs.DIRE_WOLF,
    x = 2317,
    z = 3271,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dire Wolf
spawn_npc(
    npc = Npcs.DIRE_WOLF,
    x = 2320,
    z = 3266,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dire Wolf
spawn_npc(
    npc = Npcs.DIRE_WOLF,
    x = 2335,
    z = 3276,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dire Wolf
spawn_npc(
    npc = Npcs.DIRE_WOLF,
    x = 2339,
    z = 3272,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dire Wolf
spawn_npc(
    npc = Npcs.DIRE_WOLF,
    x = 2339,
    z = 3279,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dire Wolf

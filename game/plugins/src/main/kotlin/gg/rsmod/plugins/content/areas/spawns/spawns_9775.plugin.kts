package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = Npcs.GRISH, x = 2443, z = 3051, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Grish
spawn_npc(npc = Npcs.GRUG, x = 2446, z = 3049, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Grug
spawn_npc(
    npc = Npcs.OGRE_COOK,
    x = 2450,
    z = 3056,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre cook
spawn_npc(
    npc = Npcs.OGRE_GUARD_2043,
    x = 2443,
    z = 3038,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_GUARD_2043,
    x = 2452,
    z = 3030,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_GUARD_2042,
    x = 2454,
    z = 3047,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(npc = Npcs.PILG, x = 2443, z = 3046, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Pilg
spawn_npc(
    npc = Npcs.SKOGRE,
    x = 2461,
    z = 3047,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skogre
spawn_npc(
    npc = Npcs.SKOGRE,
    x = 2466,
    z = 3052,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skogre
spawn_npc(
    npc = Npcs.SKOGRE_2056,
    x = 2469,
    z = 3039,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skogre
spawn_npc(
    npc = Npcs.SKOGRE_2057,
    x = 2480,
    z = 3046,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skogre
spawn_npc(
    npc = Npcs.UGLUG_NAR,
    x = 2442,
    z = 3049,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Uglug Nar
spawn_npc(
    npc = Npcs.ZOGRE_2052,
    x = 2464,
    z = 3045,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zogre
spawn_npc(
    npc = Npcs.ZOGRE_2051,
    x = 2464,
    z = 3049,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zogre
spawn_npc(
    npc = Npcs.ZOGRE_2053,
    x = 2467,
    z = 3049,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zogre
spawn_npc(
    npc = Npcs.ZOGRE_2054,
    x = 2468,
    z = 3046,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zogre
spawn_npc(
    npc = Npcs.ZOGRE_2055,
    x = 2470,
    z = 3055,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zogre
spawn_npc(
    npc = Npcs.ZOGRE_2045,
    x = 2482,
    z = 3046,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zogre
spawn_npc(npc = Npcs.ZOGRE, x = 2486, z = 3048, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Zogre

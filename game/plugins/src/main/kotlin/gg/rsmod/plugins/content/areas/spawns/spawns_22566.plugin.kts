package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GOLDEN_WARBLER,
    x = 5660,
    z = 2475,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Golden warbler
spawn_npc(
    npc = Npcs.GOLDEN_WARBLER,
    x = 5664,
    z = 2482,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Golden warbler
spawn_npc(
    npc = Npcs.GOLDEN_WARBLER,
    x = 5665,
    z = 2488,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Golden warbler
spawn_npc(
    npc = Npcs.GOLDEN_WARBLER,
    x = 5667,
    z = 2477,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Golden warbler
spawn_npc(
    npc = Npcs.GOLDEN_WARBLER,
    x = 5671,
    z = 2486,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Golden warbler

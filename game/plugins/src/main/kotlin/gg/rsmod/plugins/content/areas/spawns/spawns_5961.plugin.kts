package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1498,
    z = 4704,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1498,
    z = 4704,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1503,
    z = 4703,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1503,
    z = 4707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1506,
    z = 4705,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1497,
    z = 4698,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1493,
    z = 4703,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1491,
    z = 4708,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1495,
    z = 4708,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1505,
    z = 4705,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWOGRE,
    x = 1513,
    z = 4705,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwogre
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER,
    x = 1513,
    z = 4703,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER,
    x = 1509,
    z = 4702,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER,
    x = 1504,
    z = 4698,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER,
    x = 1503,
    z = 4699,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER,
    x = 1503,
    z = 4703,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER,
    x = 1499,
    z = 4705,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER,
    x = 1495,
    z = 4707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER_8777,
    x = 1491,
    z = 4703,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER_8777,
    x = 1496,
    z = 4700,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER_8777,
    x = 1503,
    z = 4698,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_HAND_CANNONEER_8777,
    x = 1511,
    z = 4697,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf hand cannoneer
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1511,
    z = 4701,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1515,
    z = 4704,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1515,
    z = 4709,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1512,
    z = 4712,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1507,
    z = 4710,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1501,
    z = 4706,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1493,
    z = 4704,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1494,
    z = 4705,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1495,
    z = 4709,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF_8778,
    x = 1500,
    z = 4709,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.DAGANNOTH_2455,
    x = 2432,
    z = 10088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth
spawn_npc(
    npc = Npcs.DAGANNOTH_2455,
    x = 2432,
    z = 10088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth
spawn_npc(
    npc = Npcs.DAGANNOTH_2455,
    x = 2432,
    z = 10088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth
spawn_npc(
    npc = Npcs.DAGANNOTH_2455,
    x = 2432,
    z = 10088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth
spawn_npc(
    npc = Npcs.DAGANNOTH_2455,
    x = 2432,
    z = 10088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CAPTAIN_KLEMFOODLE,
    x = 2970,
    z = 2973,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Captain Klemfoodle
spawn_npc(
    npc = Npcs.DOTMATRIX,
    x = 2948,
    z = 2956,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dotmatrix
spawn_npc(
    npc = Npcs.MAVERICK,
    x = 2949,
    z = 2956,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Maverick
spawn_npc(
    npc = Npcs.MONKEY,
    x = 2948,
    z = 2970,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.MONKEY,
    x = 2948,
    z = 3001,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.MONKEY,
    x = 2950,
    z = 2997,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.MONKEY,
    x = 2952,
    z = 2973,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.MONKEY,
    x = 2953,
    z = 2965,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey

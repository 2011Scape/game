package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.RUBY_HARVEST,
    x = 5314,
    z = 2397,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ruby harvest
spawn_npc(
    npc = Npcs.RUBY_HARVEST,
    x = 5318,
    z = 2409,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ruby harvest
spawn_npc(
    npc = Npcs.RUBY_HARVEST,
    x = 5319,
    z = 2383,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ruby harvest

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.JACKAL,
    x = 3331,
    z = 2940,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3332,
    z = 2938,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3333,
    z = 2940,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3343,
    z = 2895,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3345,
    z = 2896,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3346,
    z = 2894,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3348,
    z = 2894,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3376,
    z = 2907,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3376,
    z = 2934,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3378,
    z = 2905,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3378,
    z = 2906,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3378,
    z = 2908,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3378,
    z = 2933,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3378,
    z = 2935,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.MONKEY_2301,
    x = 3347,
    z = 2942,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.RUG_MERCHANT_2293,
    x = 3349,
    z = 2942,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rug merchant
spawn_npc(
    npc = Npcs.WINGSTONE,
    x = 3379,
    z = 2893,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wingstone

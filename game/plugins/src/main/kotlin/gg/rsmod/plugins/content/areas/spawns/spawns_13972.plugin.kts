package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.KALPHITE_WORKER,
    x = 3489,
    z = 9509,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Worker
spawn_npc(
    npc = Npcs.KALPHITE_WORKER,
    x = 3499,
    z = 9515,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Worker
spawn_npc(
    npc = Npcs.KALPHITE_WORKER,
    x = 3505,
    z = 9527,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Worker
spawn_npc(
    npc = Npcs.KALPHITE_WORKER,
    x = 3510,
    z = 9521,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Worker
spawn_npc(
    npc = Npcs.KALPHITE_WORKER,
    x = 3490,
    z = 9526,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Worker
spawn_npc(
    npc = Npcs.KALPHITE_WORKER,
    x = 3475,
    z = 9524,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Worker
spawn_npc(
    npc = Npcs.KALPHITE_WORKER,
    x = 3465,
    z = 9505,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Worker
spawn_npc(
    npc = Npcs.KALPHITE_WORKER,
    x = 3479,
    z = 9501,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Worker
spawn_npc(
    npc = Npcs.KALPHITE_WORKER,
    x = 3487,
    z = 9491,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Worker
spawn_npc(
    npc = Npcs.KALPHITE_SOLDIER,
    x = 3472,
    z = 9501,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Soldier
spawn_npc(
    npc = Npcs.KALPHITE_SOLDIER,
    x = 3495,
    z = 9494,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Soldier
spawn_npc(
    npc = Npcs.KALPHITE_SOLDIER,
    x = 3479,
    z = 9484,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Soldier
spawn_npc(
    npc = Npcs.KALPHITE_SOLDIER,
    x = 3500,
    z = 9477,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Soldier
spawn_npc(
    npc = Npcs.KALPHITE_GUARDIAN,
    x = 3468,
    z = 9486,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Guardian
spawn_npc(
    npc = Npcs.KALPHITE_GUARDIAN,
    x = 3467,
    z = 9480,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Guardian
spawn_npc(
    npc = Npcs.KALPHITE_GUARDIAN,
    x = 3505,
    z = 9496,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Guardian
spawn_npc(
    npc = Npcs.KALPHITE_GUARDIAN,
    x = 3513,
    z = 9496,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Guardian
spawn_npc(
    npc = Npcs.KALPHITE_GUARDIAN_1157,
    x = 3480,
    z = 9508,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Guardian
spawn_npc(
    npc = Npcs.KALPHITE_GUARDIAN_1157,
    x = 3495,
    z = 9505,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Guardian
spawn_npc(
    npc = Npcs.KALPHITE_GUARDIAN_1157,
    x = 3492,
    z = 9490,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Guardian
spawn_npc(
    npc = Npcs.KALPHITE_GUARDIAN_1157,
    x = 3472,
    z = 9497,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Guardian
spawn_npc(
    npc = Npcs.KALPHITE_QUEEN,
    x = 3484,
    z = 9491,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Queen
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3487,
    z = 9526,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3474,
    z = 9519,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3465,
    z = 9512,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3489,
    z = 9501,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3494,
    z = 9501,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3480,
    z = 9489,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3483,
    z = 9477,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3494,
    z = 9476,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3473,
    z = 9487,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3476,
    z = 9496,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3473,
    z = 9501,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3481,
    z = 9504,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3488,
    z = 9514,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3497,
    z = 9501,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva
spawn_npc(
    npc = Npcs.KALPHITE_LARVA,
    x = 3486,
    z = 9490,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kalphite Larva

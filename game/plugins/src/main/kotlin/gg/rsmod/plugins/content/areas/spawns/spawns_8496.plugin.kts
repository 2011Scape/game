package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.TRADER_CREWMEMBER,
    x = 2144,
    z = 3122,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Trader Crewmember
spawn_npc(
    npc = Npcs.TRADER_CREWMEMBER_4654,
    x = 2145,
    z = 3122,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Trader Crewmember
spawn_npc(
    npc = Npcs.TYRAS_GUARD_4649,
    x = 2154,
    z = 3121,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tyras guard
spawn_npc(
    npc = Npcs.TYRAS_GUARD_4649,
    x = 2154,
    z = 3123,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tyras guard
spawn_npc(
    npc = Npcs.TYRAS_GUARD_4649,
    x = 2159,
    z = 3125,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tyras guard
spawn_npc(
    npc = Npcs.TYRAS_GUARD_4649,
    x = 2160,
    z = 3122,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tyras guard
spawn_npc(
    npc = Npcs.TYRAS_GUARD_4649,
    x = 2141,
    z = 3122,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tyras guard
spawn_npc(
    npc = Npcs.TYRAS_GUARD_4649,
    x = 2148,
    z = 3122,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tyras guard

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 3157, x = 3682, z = 2948, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Bill Teach added by trent
spawn_npc(npc = 5667, x = 3679, z = 2965, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Brother Tranquility
spawn_npc(npc = 14318, x = 3666, z = 2981, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Sir Tendeth
spawn_npc(npc = 14342, x = 3675, z = 2975, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Pirate
spawn_npc(npc = 14344, x = 3675, z = 2985, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Pirate
spawn_npc(
    npc = Npcs._50_SHIPS_MUFASSAH,
    x = 3666,
    z = 2992,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // 50 Ships Mufassah
spawn_npc(
    npc = Npcs.BANKER_PIRATE_MALE_EYEPATCH_3198,
    x = 3682,
    z = 2981,
    height = 0,
    direction = Direction.NORTH,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.BANKER_PIRATE_MALE_HAT_3199,
    x = 3682,
    z = 2982,
    height = 0,
    direction = Direction.NORTH,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.BANKER_PIRATE_MALE_EYEPATCH_3198,
    x = 3682,
    z = 2983,
    height = 0,
    direction = Direction.NORTH,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.BARD_ROBERTS,
    x = 3667,
    z = 2984,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bard Roberts
spawn_npc(
    npc = Npcs.BRASS_HAND_HARRY_7857,
    x = 3668,
    z = 2995,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Brass Hand Harry
spawn_npc(
    npc = Npcs.CHARLEY,
    x = 3673,
    z = 2970,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Charley
spawn_npc(
    npc = Npcs.FLIPPERS_MCGRAW,
    x = 3668,
    z = 2984,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flippers McGraw
spawn_npc(
    npc = Npcs.JOE_3163,
    x = 3666,
    z = 2990,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Joe
spawn_npc(npc = Npcs.MAMA, x = 3663, z = 2980, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Mama
spawn_npc(npc = Npcs.MIKE, x = 3695, z = 2978, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Mike
spawn_npc(
    npc = Npcs.PATCHY,
    x = 3677,
    z = 2978,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Patchy
spawn_npc(
    npc = Npcs.PIRATE_3170,
    x = 3655,
    z = 2973,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3172,
    x = 3656,
    z = 2960,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3167,
    x = 3658,
    z = 2952,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3167,
    x = 3664,
    z = 2995,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3172,
    x = 3666,
    z = 2977,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3169,
    x = 3666,
    z = 2983,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3176,
    x = 3667,
    z = 2963,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3171,
    x = 3667,
    z = 2993,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3168,
    x = 3668,
    z = 2994,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3170,
    x = 3670,
    z = 2983,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3174,
    x = 3671,
    z = 2978,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3167,
    x = 3672,
    z = 2967,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3174,
    x = 3675,
    z = 2988,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3171,
    x = 3676,
    z = 2991,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3175,
    x = 3678,
    z = 3003,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3175,
    x = 3683,
    z = 2971,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3168,
    x = 3690,
    z = 2979,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3169,
    x = 3691,
    z = 2977,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3168,
    x = 3691,
    z = 2984,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3167,
    x = 3695,
    z = 2950,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3167,
    x = 3695,
    z = 2969,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3171,
    x = 3697,
    z = 2990,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3177,
    x = 3649,
    z = 3004,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3178,
    x = 3653,
    z = 3006,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3172,
    x = 3656,
    z = 2992,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3179,
    x = 3660,
    z = 3005,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3170,
    x = 3663,
    z = 2978,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3175,
    x = 3664,
    z = 2992,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3169,
    x = 3665,
    z = 2983,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3176,
    x = 3665,
    z = 2990,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3173,
    x = 3665,
    z = 2995,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3180,
    x = 3665,
    z = 3006,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3172,
    x = 3666,
    z = 2976,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3168,
    x = 3670,
    z = 2979,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3174,
    x = 3670,
    z = 2983,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3181,
    x = 3673,
    z = 3006,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3167,
    x = 3675,
    z = 2954,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3175,
    x = 3676,
    z = 2949,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3168,
    x = 3678,
    z = 2957,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3173,
    x = 3679,
    z = 2950,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3172,
    x = 3682,
    z = 2947,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3182,
    x = 3682,
    z = 3006,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3171,
    x = 3683,
    z = 2954,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3171,
    x = 3684,
    z = 2949,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3176,
    x = 3686,
    z = 2955,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3183,
    x = 3688,
    z = 3006,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3184,
    x = 3692,
    z = 3005,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3173,
    x = 3698,
    z = 2997,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3185,
    x = 3701,
    z = 3006,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_3186,
    x = 3710,
    z = 3004,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(npc = Npcs.RAT, x = 3682, z = 2948, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat
spawn_npc(npc = Npcs.RAT, x = 3688, z = 2947, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat
spawn_npc(npc = Npcs.RAT, x = 3679, z = 2949, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat
spawn_npc(npc = Npcs.SMITH, x = 3689, z = 2954, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Smith
spawn_npc(
    npc = Npcs.STANKY_MORGAN,
    x = 3666,
    z = 2984,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Stanky Morgan

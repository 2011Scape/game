package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.WARPED_COCKROACH,
    x = 3973,
    z = 5551,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped cockroach
spawn_npc(
    npc = Npcs.WARPED_COCKROACH,
    x = 3970,
    z = 5556,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped cockroach
spawn_npc(
    npc = Npcs.CORPSE_SPIDER,
    x = 3994,
    z = 5552,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Corpse Spider
spawn_npc(
    npc = Npcs.CORPSE_SPIDER,
    x = 3988,
    z = 5553,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Corpse Spider
spawn_npc(
    npc = Npcs.CORPSE_SPIDER,
    x = 3982,
    z = 5551,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Corpse Spider
spawn_npc(
    npc = Npcs.WARPED_FLY,
    x = 4001,
    z = 5541,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped fly
spawn_npc(
    npc = Npcs.WARPED_FLY,
    x = 4001,
    z = 5547,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped fly
spawn_npc(
    npc = Npcs.WARPED_FLY,
    x = 3996,
    z = 5545,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped fly
spawn_npc(
    npc = Npcs.WARPED_FLY,
    x = 3992,
    z = 5540,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped fly
spawn_npc(
    npc = Npcs.WARPED_FLY,
    x = 3989,
    z = 5547,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped fly
spawn_npc(
    npc = Npcs.CRAWLING_CORPSE_TORSO,
    x = 4011,
    z = 5542,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crawling corpse torso
spawn_npc(
    npc = Npcs.CRAWLING_CORPSE_TORSO_7917,
    x = 4009,
    z = 5543,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crawling corpse torso
spawn_npc(
    npc = Npcs.CRAWLING_CORPSE_TORSO_7917,
    x = 4015,
    z = 5544,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crawling corpse torso
spawn_npc(
    npc = Npcs.CRAWLING_CORPSE_TORSO_7918,
    x = 4009,
    z = 5542,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crawling corpse torso
spawn_npc(
    npc = Npcs.CRAWLING_CORPSE_TORSO_7918,
    x = 4013,
    z = 5546,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crawling corpse torso
spawn_npc(
    npc = Npcs.CRAWLING_CORPSE_TORSO_7919,
    x = 4010,
    z = 5543,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crawling corpse torso
spawn_npc(
    npc = Npcs.WARPED_RAT,
    x = 4026,
    z = 5542,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped rat
spawn_npc(
    npc = Npcs.WARPED_RAT,
    x = 4020,
    z = 5547,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped rat
spawn_npc(
    npc = Npcs.SKELETON_7921,
    x = 4021,
    z = 5523,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_7921,
    x = 4028,
    z = 5528,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_7921,
    x = 4020,
    z = 5532,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_7921,
    x = 4024,
    z = 5536,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.WARPED_BAT,
    x = 4014,
    z = 5509,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped bat
spawn_npc(
    npc = Npcs.WARPED_BAT,
    x = 4019,
    z = 5510,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped bat
spawn_npc(
    npc = Npcs.WARPED_BAT,
    x = 4024,
    z = 5507,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped bat
spawn_npc(
    npc = Npcs.WARPED_BAT,
    x = 4022,
    z = 5515,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warped bat

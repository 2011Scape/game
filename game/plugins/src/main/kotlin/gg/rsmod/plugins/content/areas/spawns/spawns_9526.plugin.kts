package gg.rsmod.plugins.content.areas.spawns

/**
 * North-west gnome stronghold (Swamp enclosure)
 */

spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2409, z = 3514, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2407, z = 3516, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2424, z = 3514, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2411, z = 3512, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2412, z = 3519, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2413, z = 3511, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2415, z = 3518, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2417, z = 3508, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2421, z = 3509, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2428, z = 3510, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2416, z = 3512, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2417, z = 3512, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2417, z = 3515, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2417, z = 3516, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2418, z = 3511, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2418, z = 3517, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2421, z = 3519, respawnCycles = 90)
spawn_item(item = Items.SWAMP_TOAD, amount = 1, x = 2424, z = 3517, respawnCycles = 90)

spawn_npc(npc = 2393, x = 2395, z = 3481, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Mysterious ghost
spawn_npc(npc = 3828, x = 2370, z = 3483, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Devin Mendelberg
spawn_npc(npc = Npcs.ANITA, x = 2390, z = 3514, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Anita
spawn_npc(
    npc = Npcs.BARMAN,
    x = 2417,
    z = 3498,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barman
spawn_npc(
    npc = Npcs.BURKOR,
    x = 2420,
    z = 3474,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Burkor
spawn_npc(
    npc = Npcs.BUTTERFLY_156,
    x = 2371,
    z = 3460,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY_156,
    x = 2372,
    z = 3456,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY_155,
    x = 2374,
    z = 3469,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY_155,
    x = 2376,
    z = 3466,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY_156,
    x = 2422,
    z = 3467,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY_154,
    x = 2422,
    z = 3489,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.CHEERLEADER,
    x = 2395,
    z = 3495,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cheerleader
spawn_npc(
    npc = Npcs.CHEERLEADER,
    x = 2397,
    z = 3495,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cheerleader
spawn_npc(
    npc = Npcs.DAMWIN,
    x = 2417,
    z = 3496,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Damwin
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2383,
    z = 3509,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2395,
    z = 3500,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2400,
    z = 3514,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2417,
    z = 3483,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2417, z = 3493, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2418,
    z = 3485,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2418, z = 3495, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_BALL_REFEREE,
    x = 2384,
    z = 3488,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome ball referee
spawn_npc(
    npc = Npcs.GNOME_BALLER_629,
    x = 2391,
    z = 3484,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER_629,
    x = 2391,
    z = 3487,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER_629,
    x = 2391,
    z = 3489,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER_629,
    x = 2391,
    z = 3492,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER_625,
    x = 2396,
    z = 3485,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER_625,
    x = 2396,
    z = 3487,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER_625,
    x = 2396,
    z = 3489,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER_625,
    x = 2396,
    z = 3491,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER,
    x = 2400,
    z = 3483,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER,
    x = 2400,
    z = 3485,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER,
    x = 2400,
    z = 3487,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER,
    x = 2400,
    z = 3489,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER,
    x = 2400,
    z = 3491,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_BALLER,
    x = 2400,
    z = 3493,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome baller
spawn_npc(
    npc = Npcs.GNOME_CHILD,
    x = 2392,
    z = 3475,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome child
spawn_npc(
    npc = Npcs.GNOME_CHILD,
    x = 2394,
    z = 3506,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome child
spawn_npc(
    npc = Npcs.GNOME_CHILD,
    x = 2396,
    z = 3471,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome child
spawn_npc(
    npc = Npcs.GNOME_CHILD,
    x = 2405,
    z = 3499,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome child
spawn_npc(
    npc = Npcs.GNOME_CHILD,
    x = 2416,
    z = 3487,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome child
spawn_npc(
    npc = Npcs.GNOME_COACH,
    x = 2406,
    z = 3498,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome Coach
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2409,
    z = 3470,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2416,
    z = 3466,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2420,
    z = 3466,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_TROOP,
    x = 2384,
    z = 3481,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome troop
spawn_npc(
    npc = Npcs.GNOME_TROOP,
    x = 2388,
    z = 3473,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome troop
spawn_npc(
    npc = Npcs.GNOME_TROOP_480,
    x = 2409,
    z = 3507,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome troop
spawn_npc(
    npc = Npcs.GNOME_WINGER,
    x = 2393,
    z = 3483,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome winger
spawn_npc(
    npc = Npcs.GNOME_WINGER,
    x = 2393,
    z = 3493,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome winger
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2378,
    z = 3482,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2379,
    z = 3482,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2381,
    z = 3495,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2383,
    z = 3496,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2384,
    z = 3497,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2391,
    z = 3476,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2402,
    z = 3507,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2406,
    z = 3476,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2410,
    z = 3496,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2421,
    z = 3481,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2422,
    z = 3485,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2382,
    z = 3506,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2397,
    z = 3514,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2418,
    z = 3472,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.SARBLE,
    x = 2417,
    z = 3517,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sarble
spawn_npc(
    npc = Npcs.WURBEL,
    x = 2395,
    z = 3477,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wurbel

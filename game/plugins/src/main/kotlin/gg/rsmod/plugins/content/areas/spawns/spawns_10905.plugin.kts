package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2746,
    z = 9831,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2743,
    z = 9841,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2735,
    z = 9843,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2725,
    z = 9845,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2715,
    z = 9844,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2708,
    z = 9841,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2699,
    z = 9839,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2695,
    z = 9830,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2695,
    z = 9824,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta
spawn_npc(
    npc = Npcs.ICE_SPIDER,
    x = 2691,
    z = 9815,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice spider added by jazneta

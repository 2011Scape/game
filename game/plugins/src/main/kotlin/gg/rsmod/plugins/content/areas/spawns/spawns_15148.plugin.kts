package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 5668, x = 3787, z = 2824, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Brother Tranquility
spawn_npc(npc = 5669, x = 3787, z = 2827, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Monk
spawn_npc(npc = 5670, x = 3786, z = 2827, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Monk
spawn_npc(npc = 5671, x = 3788, z = 2826, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Monk
spawn_npc(npc = 5672, x = 3788, z = 2825, height = 2, walkRadius = 5, direction = Direction.NORTH, static = false) // Monk
spawn_npc(
    npc = Npcs.MONK_5611,
    x = 3784,
    z = 2824,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monk
spawn_npc(
    npc = Npcs.MONK_5610,
    x = 3785,
    z = 2827,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monk
spawn_npc(
    npc = Npcs.MONK_5609,
    x = 3786,
    z = 2824,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monk
spawn_npc(
    npc = Npcs.MONK_5608,
    x = 3786,
    z = 2827,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monk
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3779,
    z = 2826,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3789,
    z = 2817,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3794,
    z = 2821,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3808,
    z = 2823,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3812,
    z = 2861,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3813,
    z = 2822,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3818,
    z = 2862,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3819,
    z = 2870,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3821,
    z = 2824,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.PUFFIN,
    x = 3831,
    z = 2828,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Puffin
spawn_npc(
    npc = Npcs.SOREBONES,
    x = 3808,
    z = 2845,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES_5628,
    x = 3810,
    z = 2843,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES,
    x = 3812,
    z = 2846,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES_5628,
    x = 3813,
    z = 2844,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES,
    x = 3815,
    z = 2844,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES,
    x = 3819,
    z = 2839,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES,
    x = 3819,
    z = 2851,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES_5628,
    x = 3821,
    z = 2837,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES,
    x = 3819,
    z = 2838,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES_5628,
    x = 3820,
    z = 2840,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES_5628,
    x = 3820,
    z = 2851,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES,
    x = 3823,
    z = 2836,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES_5628,
    x = 3823,
    z = 2839,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.SOREBONES,
    x = 3823,
    z = 2846,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sorebones
spawn_npc(
    npc = Npcs.TOOL_LEPRECHAUN_7560,
    x = 3796,
    z = 2837,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tool leprechaun
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5635,
    x = 3784,
    z = 2843,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5646,
    x = 3792,
    z = 2830,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5634,
    x = 3793,
    z = 2851,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5644,
    x = 3795,
    z = 2845,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5642,
    x = 3796,
    z = 2858,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5641,
    x = 3799,
    z = 2855,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5650,
    x = 3800,
    z = 2835,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5637,
    x = 3803,
    z = 2843,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5665,
    x = 3803,
    z = 2848,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5638,
    x = 3810,
    z = 2833,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5629,
    x = 3811,
    z = 2826,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5633,
    x = 3815,
    z = 2859,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5640,
    x = 3817,
    z = 2824,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5639,
    x = 3824,
    z = 2857,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5631,
    x = 3827,
    z = 2833,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5656,
    x = 3794,
    z = 2873,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5658,
    x = 3804,
    z = 2873,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5647,
    x = 3803,
    z = 2872,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5659,
    x = 3805,
    z = 2874,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate
spawn_npc(
    npc = Npcs.ZOMBIE_PIRATE_5661,
    x = 3794,
    z = 2874,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie pirate

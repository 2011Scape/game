package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.ABYSSAL_GUARDIAN,
    x = 3008,
    z = 4768,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Abyssal guardian
spawn_npc(
    npc = Npcs.ABYSSAL_GUARDIAN,
    x = 3008,
    z = 4768,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Abyssal guardian

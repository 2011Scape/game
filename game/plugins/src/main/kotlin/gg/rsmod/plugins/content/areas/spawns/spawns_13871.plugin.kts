package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BANDIT_LOOTER,
    x = 3488,
    z = 3028,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit looter
spawn_npc(
    npc = Npcs.BANDIT_LOOTER_7493,
    x = 3489,
    z = 3032,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit looter
spawn_npc(
    npc = Npcs.BANDIT_LOOTER_7495,
    x = 3490,
    z = 3040,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit looter
spawn_npc(
    npc = Npcs.BANDIT_LOOTER_7494,
    x = 3496,
    z = 3042,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit looter
spawn_npc(
    npc = Npcs.BANDIT_LOOTER_7496,
    x = 3500,
    z = 3040,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit looter

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.EXPERIMENT,
    x = 3553,
    z = 9947,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Wolf-woman)
spawn_npc(
    npc = Npcs.EXPERIMENT,
    x = 3530,
    z = 9968,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Wolf-woman)
spawn_npc(
    npc = Npcs.EXPERIMENT,
    x = 3554,
    z = 9927,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Wolf-woman)
spawn_npc(
    npc = Npcs.EXPERIMENT,
    x = 3524,
    z = 9932,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Wolf-woman)
spawn_npc(
    npc = Npcs.EXPERIMENT,
    x = 3493,
    z = 9925,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Wolf-woman)
spawn_npc(
    npc = Npcs.EXPERIMENT,
    x = 3480,
    z = 9943,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Wolf-woman)
spawn_npc(
    npc = Npcs.EXPERIMENT_1678,
    x = 3549,
    z = 9955,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Sheep-dog)
spawn_npc(
    npc = Npcs.EXPERIMENT_1678,
    x = 3565,
    z = 9950,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Sheep-dog)
spawn_npc(
    npc = Npcs.EXPERIMENT_1678,
    x = 3552,
    z = 9940,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Sheep-dog)
spawn_npc(
    npc = Npcs.EXPERIMENT_1678,
    x = 3534,
    z = 9925,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Sheep-dog)
spawn_npc(
    npc = Npcs.EXPERIMENT_1678,
    x = 3498,
    z = 9937,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Sheep-dog)
spawn_npc(
    npc = Npcs.EXPERIMENT_1678,
    x = 3488,
    z = 9946,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Sheep-dog)
spawn_npc(
    npc = Npcs.EXPERIMENT_1678,
    x = 3473,
    z = 9943,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Sheep-dog)
spawn_npc(
    npc = Npcs.EXPERIMENT_1677,
    x = 3566,
    z = 9960,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Spider-man)
spawn_npc(
    npc = Npcs.EXPERIMENT_1677,
    x = 3539,
    z = 9958,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Spider-man)
spawn_npc(
    npc = Npcs.EXPERIMENT_1677,
    x = 3546,
    z = 9932,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Spider-man)
spawn_npc(
    npc = Npcs.EXPERIMENT_1677,
    x = 3487,
    z = 9933,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Spider-man)
spawn_npc(
    npc = Npcs.EXPERIMENT_1677,
    x = 3474,
    z = 9932,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Experiment(Spider-man)

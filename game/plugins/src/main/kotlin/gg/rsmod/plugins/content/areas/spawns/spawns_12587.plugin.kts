package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.JACKAL,
    x = 3160,
    z = 2800,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal

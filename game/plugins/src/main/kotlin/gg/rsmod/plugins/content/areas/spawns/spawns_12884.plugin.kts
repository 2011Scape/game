package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.JELLY_1639,
    x = 3208,
    z = 5400,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jelly

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.WINELDA,
    x = 2656,
    z = 9875,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Winelda added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2661,
    z = 9881,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_93,
    x = 2658,
    z = 9886,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2655,
    z = 9892,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2651,
    z = 9890,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2641,
    z = 9889,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2633,
    z = 9888,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.LESSER_DEMON,
    x = 2631,
    z = 9884,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lesser demon added by jazneta
spawn_npc(
    npc = Npcs.LESSER_DEMON,
    x = 2635,
    z = 9882,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lesser demon added by jazneta
spawn_npc(
    npc = Npcs.LESSER_DEMON,
    x = 2632,
    z = 9877,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lesser demon added by jazneta
spawn_npc(
    npc = Npcs.GUARDIAN_OF_ARMADYL_275,
    x = 2638,
    z = 9899,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guardian of Armadyl added by jazneta
spawn_npc(
    npc = Npcs.GUARDIAN_OF_ARMADYL,
    x = 2648,
    z = 9904,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guardian of Armadyl added by jazneta
spawn_npc(
    npc = Npcs.GUARDIAN_OF_ARMADYL_275,
    x = 2645,
    z = 9908,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guardian of Armadyl added by jazneta
spawn_npc(
    npc = Npcs.GUARDIAN_OF_ARMADYL,
    x = 2649,
    z = 9913,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guardian of Armadyl added by jazneta
spawn_npc(
    npc = Npcs.GUARDIAN_OF_ARMADYL_275,
    x = 2637,
    z = 9910,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guardian of Armadyl added by jazneta

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.MENAPHITE_GUARD,
    x = 3249,
    z = 2727,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Menaphite guard
spawn_npc(
    npc = Npcs.MENAPHITE_GUARD,
    x = 3249,
    z = 2729,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Menaphite guard
spawn_npc(
    npc = Npcs.MENAPHITE_GUARD,
    x = 3249,
    z = 2731,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Menaphite guard

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BOGROG,
    x = 2523,
    z = 3057,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bogrog
spawn_npc(
    npc = Npcs.CITY_GUARD,
    x = 2541,
    z = 3029,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // City guard
spawn_npc(
    npc = Npcs.CITY_GUARD,
    x = 2541,
    z = 3034,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // City guard
spawn_npc(
    npc = Npcs.CITY_GUARD,
    x = 2543,
    z = 3031,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // City guard
spawn_npc(
    npc = Npcs.ENCLAVE_GUARD,
    x = 2507,
    z = 3036,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Enclave guard
spawn_npc(
    npc = Npcs.ENCLAVE_GUARD,
    x = 2507,
    z = 3040,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Enclave guard
spawn_npc(npc = Npcs.OGRE, x = 2510, z = 3044, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3419,
    x = 2513,
    z = 3026,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3451,
    x = 2520,
    z = 3046,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_8658,
    x = 2526,
    z = 3039,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3451,
    x = 2529,
    z = 3032,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3419,
    x = 2539,
    z = 3018,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_8658,
    x = 2541,
    z = 3016,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3419,
    x = 2550,
    z = 3043,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(npc = Npcs.OGRE, x = 2550, z = 3046, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3451,
    x = 2553,
    z = 3047,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_CHIEFTAIN,
    x = 2505,
    z = 3032,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre chieftain
spawn_npc(
    npc = Npcs.OGRE_CHIEFTAIN,
    x = 2512,
    z = 3022,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre chieftain
spawn_npc(
    npc = Npcs.OGRE_CHIEFTAIN,
    x = 2518,
    z = 3041,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre chieftain
spawn_npc(
    npc = Npcs.OGRE_CHIEFTAIN,
    x = 2528,
    z = 3044,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre chieftain
spawn_npc(
    npc = Npcs.OGRE_GUARD_860,
    x = 2501,
    z = 3012,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_GUARD_860,
    x = 2503,
    z = 3011,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_GUARD_859,
    x = 2505,
    z = 3062,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_GUARD_859,
    x = 2507,
    z = 3062,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_GUARD_861,
    x = 2526,
    z = 3018,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_GUARD_861,
    x = 2529,
    z = 3019,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_GUARD_858,
    x = 2549,
    z = 3029,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_GUARD_858,
    x = 2551,
    z = 3031,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre guard
spawn_npc(
    npc = Npcs.OGRE_MERCHANT,
    x = 2528,
    z = 3048,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre merchant
spawn_npc(
    npc = Npcs.OGRE_TRADER_875,
    x = 2513,
    z = 3034,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre trader
spawn_npc(
    npc = Npcs.OGRE_TRADER,
    x = 2518,
    z = 3035,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre trader
spawn_npc(
    npc = Npcs.OGRE_TRADER_876,
    x = 2525,
    z = 3043,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre trader

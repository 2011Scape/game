package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 5077, x = 2512, z = 2864, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Man
spawn_npc(
    npc = Npcs.FLYING_BUGS,
    x = 2539,
    z = 2838,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flying bugs
spawn_npc(
    npc = Npcs.FLYING_BUGS,
    x = 2542,
    z = 2832,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flying bugs
spawn_npc(
    npc = Npcs.FLYING_BUGS,
    x = 2532,
    z = 2836,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flying bugs
spawn_npc(
    npc = Npcs.FLYING_BUGS,
    x = 2541,
    z = 2837,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flying bugs
spawn_npc(
    npc = Npcs.FLYING_BUGS,
    x = 2535,
    z = 2832,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flying bugs
spawn_npc(
    npc = Npcs.FLYING_BUGS,
    x = 2543,
    z = 2832,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flying bugs
spawn_npc(
    npc = Npcs.FLYING_BUGS,
    x = 2536,
    z = 2839,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flying bugs
spawn_npc(
    npc = Npcs.FLYING_BUGS,
    x = 2537,
    z = 2840,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flying bugs
spawn_npc(
    npc = Npcs.FLYING_BUGS,
    x = 2533,
    z = 2836,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flying bugs
spawn_npc(
    npc = Npcs.BABY_PLATYPUS_7024,
    x = 2523,
    z = 2823,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby platypus
spawn_npc(
    npc = Npcs.BABY_PLATYPUS_7025,
    x = 2537,
    z = 2820,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby platypus
spawn_npc(
    npc = Npcs.BABY_PLATYPUS_7026,
    x = 2543,
    z = 2822,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby platypus
spawn_npc(
    npc = Npcs.BALNEA,
    x = 2557,
    z = 2836,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Balnea
spawn_npc(
    npc = Npcs.CHIEF_TESS,
    x = 2547,
    z = 2852,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chief Tess
spawn_npc(npc = Npcs.DAWG, x = 2549, z = 2852, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Dawg
spawn_npc(
    npc = Npcs.DISEASED_KEBBIT,
    x = 2503,
    z = 2824,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Diseased kebbit
spawn_npc(
    npc = Npcs.DISEASED_KEBBIT,
    x = 2518,
    z = 2824,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Diseased kebbit
spawn_npc(
    npc = Npcs.DISEASED_KEBBIT,
    x = 2534,
    z = 2821,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Diseased kebbit
spawn_npc(
    npc = Npcs.DISEASED_KEBBIT,
    x = 2548,
    z = 2823,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Diseased kebbit
spawn_npc(npc = Npcs.FRAWD, x = 2545, z = 2836, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Frawd
spawn_npc(
    npc = Npcs.OGRESS,
    x = 2499,
    z = 2860,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogress
spawn_npc(
    npc = Npcs.OGRESS_7082,
    x = 2516,
    z = 2858,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogress
spawn_npc(
    npc = Npcs.BANKER_OGRESS_7050,
    x = 2553,
    z = 2837,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogress banker
spawn_npc(
    npc = Npcs.BANKER_OGRESS_7049,
    x = 2553,
    z = 2839,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogress banker
spawn_npc(
    npc = Npcs.OGRESS_CHAMPION,
    x = 2502,
    z = 2838,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogress champion
spawn_npc(
    npc = Npcs.OGRESS_WARRIOR,
    x = 2501,
    z = 2851,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogress warrior
spawn_npc(
    npc = Npcs.OGRESS_WARRIOR_7080,
    x = 2508,
    z = 2853,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogress warrior
spawn_npc(
    npc = Npcs.PLATYPUS_7022,
    x = 2503,
    z = 2821,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Platypus
spawn_npc(
    npc = Npcs.PLATYPUS_7021,
    x = 2510,
    z = 2822,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Platypus
spawn_npc(
    npc = Npcs.PLATYPUS_7023,
    x = 2552,
    z = 2821,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Platypus
spawn_npc(
    npc = Npcs.TROPICAL_WAGTAIL,
    x = 2510,
    z = 2876,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tropical wagtail
spawn_npc(
    npc = Npcs.TROPICAL_WAGTAIL,
    x = 2517,
    z = 2878,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tropical wagtail
spawn_npc(
    npc = Npcs.TROPICAL_WAGTAIL,
    x = 2527,
    z = 2877,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tropical wagtail
spawn_npc(
    npc = Npcs.WIMPY_BIRD,
    x = 2499,
    z = 2820,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wimpy bird
spawn_npc(
    npc = Npcs.WIMPY_BIRD,
    x = 2511,
    z = 2819,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wimpy bird
spawn_npc(
    npc = Npcs.WIMPY_BIRD,
    x = 2525,
    z = 2820,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wimpy bird
spawn_npc(
    npc = Npcs.WIMPY_BIRD,
    x = 2527,
    z = 2818,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wimpy bird
spawn_npc(
    npc = Npcs.WIMPY_BIRD,
    x = 2542,
    z = 2819,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wimpy bird
spawn_npc(
    npc = Npcs.WIMPY_BIRD,
    x = 2551,
    z = 2817,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wimpy bird
spawn_npc(
    npc = Npcs.WIMPY_BIRD,
    x = 2554,
    z = 2819,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wimpy bird
spawn_npc(
    npc = Npcs.WIMPY_BIRD,
    x = 2559,
    z = 2819,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wimpy bird

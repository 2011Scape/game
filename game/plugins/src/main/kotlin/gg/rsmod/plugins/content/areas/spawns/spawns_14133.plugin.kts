package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BARK_BLAMISH_SNAIL,
    x = 3530,
    z = 3398,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bark Blamish Snail
spawn_npc(
    npc = Npcs.BLOOD_BLAMISH_SNAIL,
    x = 3531,
    z = 3438,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Blood Blamish Snail
spawn_npc(
    npc = Npcs.BLOOD_BLAMISH_SNAIL_1233,
    x = 3534,
    z = 3428,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Blood Blamish Snail
spawn_npc(
    npc = Npcs.BLOOD_BLAMISH_SNAIL,
    x = 3558,
    z = 3453,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Blood Blamish Snail
spawn_npc(
    npc = Npcs.BRUISE_BLAMISH_SNAIL,
    x = 3520,
    z = 3407,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bruise Blamish Snail
spawn_npc(npc = Npcs.GHAST, x = 3523, z = 3436, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ghast
spawn_npc(npc = Npcs.GHAST, x = 3526, z = 3398, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ghast
spawn_npc(npc = Npcs.GHAST, x = 3531, z = 3425, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ghast
spawn_npc(npc = Npcs.GHAST, x = 3548, z = 3450, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ghast
spawn_npc(npc = Npcs.GHAST, x = 3563, z = 3444, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ghast
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3532,
    z = 3446,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3537,
    z = 3445,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3537,
    z = 3451,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3547,
    z = 3442,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3549,
    z = 3437,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3549,
    z = 3449,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3552,
    z = 3450,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3554,
    z = 3440,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3554,
    z = 3453,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3556,
    z = 3438,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3558,
    z = 3444,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.SWAMP_LIZARD,
    x = 3562,
    z = 3435,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Green salamander
spawn_npc(
    npc = Npcs.MYRE_BLAMISH_SNAIL,
    x = 3524,
    z = 3413,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Myre Blamish Snail
spawn_npc(
    npc = Npcs.MYRE_BLAMISH_SNAIL,
    x = 3530,
    z = 3441,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Myre Blamish Snail
spawn_npc(
    npc = Npcs.MYRE_BLAMISH_SNAIL,
    x = 3541,
    z = 3410,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Myre Blamish Snail
spawn_npc(
    npc = Npcs.MYRE_BLAMISH_SNAIL,
    x = 3544,
    z = 3430,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Myre Blamish Snail
spawn_npc(
    npc = Npcs.MYRE_BLAMISH_SNAIL,
    x = 3563,
    z = 3438,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Myre Blamish Snail
spawn_npc(
    npc = Npcs.OCHRE_BLAMISH_SNAIL,
    x = 3540,
    z = 3430,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ochre Blamish Snail
spawn_npc(
    npc = Npcs.OCHRE_BLAMISH_SNAIL_1234,
    x = 3545,
    z = 3440,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ochre Blamish Snail

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3294,
    z = 3612,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp
spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3298,
    z = 3607,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp
spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3300,
    z = 3617,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp
spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3305,
    z = 3627,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp
spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3308,
    z = 3588,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp
spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3311,
    z = 3633,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp
spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3312,
    z = 3621,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp
spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3313,
    z = 3607,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp
spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3314,
    z = 3630,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp
spawn_npc(
    npc = Npcs.BRIAN_ORICHARD,
    x = 3318,
    z = 3614,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Will o' the wisp

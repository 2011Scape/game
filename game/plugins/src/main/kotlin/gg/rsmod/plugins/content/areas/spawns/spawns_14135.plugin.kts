package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GHOSTLY_PIPER,
    x = 3582,
    z = 3524,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Musician

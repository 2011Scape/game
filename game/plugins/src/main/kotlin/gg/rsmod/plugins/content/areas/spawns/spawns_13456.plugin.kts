package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.SCABARAS_LANCER,
    x = 3361,
    z = 9238,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras lancer
spawn_npc(
    npc = Npcs.SCABARAS_LANCER,
    x = 3363,
    z = 9241,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras lancer
spawn_npc(
    npc = Npcs.SCABARAS_LANCER,
    x = 3373,
    z = 9241,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras lancer
spawn_npc(
    npc = Npcs.SCABARAS_LANCER,
    x = 3374,
    z = 9225,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras lancer
spawn_npc(
    npc = Npcs.SCABARAS_LANCER,
    x = 3379,
    z = 9230,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras lancer
spawn_npc(
    npc = Npcs.SCABARAS_LANCER,
    x = 3383,
    z = 9232,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras lancer
spawn_npc(
    npc = Npcs.SCABARAS_LANCER,
    x = 3387,
    z = 9229,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras lancer
spawn_npc(
    npc = Npcs.SCABARAS_LANCER,
    x = 3389,
    z = 9236,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras lancer


spawn_npc(
    npc = Npcs.SCABARAS_MAGE_6781,
    x = 3363,
    z = 9231,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras mage
spawn_npc(
    npc = Npcs.SCABARAS_MAGE,
    x = 3366,
    z = 9254,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras mage
spawn_npc(
    npc = Npcs.SCABARAS_MAGE_6781,
    x = 3369,
    z = 9254,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras mage
spawn_npc(
    npc = Npcs.SCABARAS_MAGE_6781,
    x = 3370,
    z = 9226,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras mage
spawn_npc(
    npc = Npcs.SCABARAS_MAGE_6781,
    x = 3397,
    z = 9225,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras mage
spawn_npc(
    npc = Npcs.SCABARAS_MAGE,
    x = 3398,
    z = 9232,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras mage
spawn_npc(
    npc = Npcs.SCABARAS_MAGE_6781,
    x = 3408,
    z = 9226,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras mage
spawn_npc(
    npc = Npcs.SCABARAS_MAGE,
    x = 3409,
    z = 9230,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras mage
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3358,
    z = 9233,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3363,
    z = 9246,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3369,
    z = 9233,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3373,
    z = 9246,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3377,
    z = 9244,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3381,
    z = 9225,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3389,
    z = 9241,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3391,
    z = 9230,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger
spawn_npc(
    npc = Npcs.SCABARAS_RANGER,
    x = 3391,
    z = 9237,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scabaras ranger

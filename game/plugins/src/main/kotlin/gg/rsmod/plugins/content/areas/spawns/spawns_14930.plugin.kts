package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.SPIDER_HERALD,
    x = 3743,
    z = 5280,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider herald added by trent
spawn_npc(
    npc = Npcs.ROYAL_FLYCATCHER,
    x = 3741,
    z = 5284,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Royal Fly-Catcher added by trent
spawn_npc(
    npc = Npcs.SPIDER_COURTIER,
    x = 3741,
    z = 5279,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider Courtier added by trent
spawn_npc(
    npc = Npcs.SPIDER_COURTIER,
    x = 3746,
    z = 5285,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider Courtier added by trent
spawn_npc(
    npc = Npcs.SPIDER_COURTIER,
    x = 3741,
    z = 5286,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider Courtier added by trent
spawn_npc(
    npc = Npcs.SPIDER_COURTIER,
    x = 3738,
    z = 5282,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider Courtier added by trent
spawn_npc(
    npc = Npcs.SPIDER_COURTIER,
    x = 3748,
    z = 5280,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider Courtier added by trent
spawn_npc(
    npc = Npcs.SPIDER_COURTIER,
    x = 3751,
    z = 5282,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider Courtier added by trent
spawn_npc(
    npc = Npcs.KEEPER_OF_THE_ROYAL_EGGS,
    x = 3747,
    z = 5283,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Keeper of the Royal Eggs added by trent
spawn_npc(
    npc = Npcs.SPIDER_COURTIER,
    x = 3747,
    z = 5277,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider Courtier added by trent
spawn_npc(
    npc = Npcs.SPIDER_COURTIER,
    x = 3741,
    z = 5276,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider Courtier added by trent

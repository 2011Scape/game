package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.DREZEL_7707,
    x = 3440,
    z = 9895,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Drezel
spawn_npc(
    npc = Npcs.TEMPLE_GUARDIAN,
    x = 3405,
    z = 9902,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cerberus

spawn_obj(obj = Objs.HOLY_BARRIER, x = 3440, z = 9886, height = 0, type = 10, rot = 1)

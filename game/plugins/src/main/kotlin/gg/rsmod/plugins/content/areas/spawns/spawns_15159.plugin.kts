package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GHOST_CAPTAIN,
    x = 3792,
    z = 3560,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ghost captain

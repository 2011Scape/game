package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.KHARID_SCORPION_386,
    x = 2555,
    z = 3570,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Khazard scorpion 2
spawn_npc(
    npc = Npcs.BARBARIAN_3253,
    x = 2544,
    z = 3577,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barbarian
spawn_npc(
    npc = Npcs.BARBARIAN,
    x = 2546,
    z = 3560,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barbarian
spawn_npc(
    npc = Npcs.BARBARIAN_3247,
    x = 2546,
    z = 3565,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barbarian
spawn_npc(
    npc = Npcs.BARBARIAN_3262,
    x = 2548,
    z = 3558,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barbarian
spawn_npc(
    npc = Npcs.BARBARIAN_3248,
    x = 2549,
    z = 3566,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barbarian
spawn_npc(
    npc = Npcs.BARBARIAN_3250,
    x = 2549,
    z = 3570,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barbarian
spawn_npc(
    npc = Npcs.BARBARIAN_3260,
    x = 2550,
    z = 3563,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barbarian
spawn_npc(
    npc = Npcs.BARBARIAN_GUARD,
    x = 2544,
    z = 3567,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barbarian guard
spawn_npc(
    npc = Npcs.BARBARIAN_GUARD,
    x = 2544,
    z = 3571,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barbarian guard
spawn_npc(
    npc = Npcs.CAPTAIN_CAIN,
    x = 2534,
    z = 3568,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Captain Cain
spawn_npc(
    npc = Npcs.CHICKEN_1017,
    x = 2553,
    z = 3562,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1017,
    x = 2553,
    z = 3565,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN,
    x = 2554,
    z = 3563,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.COMMANDER_CONNAD,
    x = 2536,
    z = 3577,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Commander Connad
spawn_npc(npc = Npcs.FISHING_SPOT_2724, x = 2498, z = 3547, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_2724, x = 2511, z = 3562, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_2724, x = 2516, z = 3575, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(
    npc = Npcs.GUNNJORN,
    x = 2540,
    z = 3548,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gunnjorn
spawn_npc(
    npc = Npcs.PRIVATE_PALDON,
    x = 2526,
    z = 3575,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Private Paldon
spawn_npc(
    npc = Npcs.PRIVATE_PENDRON,
    x = 2525,
    z = 3564,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Private Pendron
spawn_npc(
    npc = Npcs.PRIVATE_PIERREB,
    x = 2520,
    z = 3568,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Private Pierreb
spawn_npc(
    npc = Npcs.TALENT_SCOUT,
    x = 2544,
    z = 3550,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Talent scout

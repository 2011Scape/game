package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BARTENDER_739,
    x = 2556,
    z = 3078,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bartender
spawn_npc(
    npc = Npcs.COLONEL_RADICK,
    x = 2535,
    z = 3091,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Colonel Radick
spawn_npc(npc = Npcs.GREW, x = 2513, z = 3084, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Grew
spawn_npc(
    npc = Npcs.GUARD_CAPTAIN,
    x = 2551,
    z = 3078,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard Captain
spawn_npc(
    npc = Npcs.MUSICIAN_8706,
    x = 2526,
    z = 3096,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Musician
spawn_npc(npc = Npcs.OG, x = 2506, z = 3116, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Og
spawn_npc(
    npc = Npcs.OGRE_3419,
    x = 2496,
    z = 3092,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_8658,
    x = 2497,
    z = 3089,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3451,
    x = 2500,
    z = 3092,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3451,
    x = 2500,
    z = 3097,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3451,
    x = 2502,
    z = 3114,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3419,
    x = 2503,
    z = 3095,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3419,
    x = 2504,
    z = 3133,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(npc = Npcs.OGRE, x = 2505, z = 3111, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3451,
    x = 2507,
    z = 3108,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_8658,
    x = 2508,
    z = 3119,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(npc = Npcs.OGRE, x = 2509, z = 3087, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ogre
spawn_npc(npc = Npcs.OGRE, x = 2510, z = 3084, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ogre
spawn_npc(npc = Npcs.OGRE, x = 2513, z = 3087, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ogre
spawn_npc(npc = Npcs.OGRE, x = 2514, z = 3081, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ogre
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2540,
    z = 3102,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2542,
    z = 3094,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2543,
    z = 3088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2545,
    z = 3094,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2546,
    z = 3085,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2547,
    z = 3091,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2533,
    z = 3086,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2533,
    z = 3094,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2534,
    z = 3097,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2535,
    z = 3088,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2537,
    z = 3078,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2537,
    z = 3102,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2539,
    z = 3107,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2551,
    z = 3109,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2553,
    z = 3073,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2558,
    z = 3073,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.TOWER_GUARD,
    x = 2540,
    z = 3090,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tower guard
spawn_npc(
    npc = Npcs.TOWER_GUARD,
    x = 2545,
    z = 3113,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tower guard
spawn_npc(
    npc = Npcs.TOWER_GUARD,
    x = 2546,
    z = 3116,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tower guard
spawn_npc(
    npc = Npcs.TOWER_GUARD,
    x = 2549,
    z = 3112,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tower guard
spawn_npc(
    npc = Npcs.TOWER_GUARD,
    x = 2554,
    z = 3117,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tower guard
spawn_npc(
    npc = Npcs.WATCHMAN,
    x = 2544,
    z = 3112,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Watchman
spawn_npc(
    npc = Npcs.WATCHMAN,
    x = 2545,
    z = 3116,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Watchman
spawn_npc(
    npc = Npcs.WATCHMAN,
    x = 2549,
    z = 3112,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Watchman
spawn_npc(
    npc = Npcs.WATCHMAN,
    x = 2549,
    z = 3116,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Watchman
spawn_npc(
    npc = Npcs.WATCHTOWER_WIZARD,
    x = 2549,
    z = 3116,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Watchtower Wizard
spawn_npc(
    npc = Npcs.WIZARD_5196,
    x = 2544,
    z = 3112,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD_5195,
    x = 2544,
    z = 3117,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD_5197,
    x = 2549,
    z = 3112,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard

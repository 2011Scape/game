package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CORMORANT_9718,
    x = 3505,
    z = 3774,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cormorant
spawn_npc(
    npc = Npcs.CORMORANT_9718,
    x = 3515,
    z = 3747,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cormorant
spawn_npc(
    npc = Npcs.GULL_9717,
    x = 3515,
    z = 3760,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gull

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.MITHRIL_DRAGON,
    x = 1712,
    z = 5312,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mithril dragon
spawn_npc(
    npc = Npcs.MITHRIL_DRAGON,
    x = 1712,
    z = 5312,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mithril dragon
spawn_npc(
    npc = Npcs.CONFUSED_BARBARIAN,
    x = 1688,
    z = 5312,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Confused barbarian

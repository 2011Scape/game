package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 258, x = 2569, z = 3199, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Lady Servil added by jazneta
spawn_npc(npc = 7541, x = 2567, z = 3199, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Justin Servil added by jazneta
spawn_npc(npc = 7533, x = 2568, z = 3199, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Jeremy Servil added by jazneta
spawn_npc(npc = 265, x = 2616, z = 3167, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Jeremy Servil
spawn_npc(npc = 267, x = 2601, z = 3168, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Slave fighter
spawn_npc(npc = 7521, x = 2609, z = 3163, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Bouncer
spawn_npc(npc = 7522, x = 2609, z = 3166, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Khazard ogre
spawn_npc(npc = 7550, x = 2616, z = 3143, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Lazy Khazard Guard
spawn_npc(npc = Npcs.ANGOR, x = 2566, z = 3140, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Angor
spawn_npc(
    npc = Npcs.CITIZEN,
    x = 2565,
    z = 3149,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Citizen
spawn_npc(
    npc = Npcs.HENGRAD,
    x = 2598,
    z = 3142,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hengrad
spawn_npc(npc = Npcs.JOE, x = 2587, z = 3142, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Joe
spawn_npc(
    npc = Npcs.KELVIN,
    x = 2589,
    z = 3142,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kelvin
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_254,
    x = 2564,
    z = 3146,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7520,
    x = 2564,
    z = 3153,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7519,
    x = 2566,
    z = 3142,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_254,
    x = 2567,
    z = 3149,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7518,
    x = 2569,
    z = 3144,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_254,
    x = 2570,
    z = 3142,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2577,
    z = 3162,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7514,
    x = 2578,
    z = 3159,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7515,
    x = 2578,
    z = 3160,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7514,
    x = 2578,
    z = 3163,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7515,
    x = 2578,
    z = 3164,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2579,
    z = 3171,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2580,
    z = 3166,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2583,
    z = 3149,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD,
    x = 2584,
    z = 3142,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7504,
    x = 2588,
    z = 3174,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7511,
    x = 2589,
    z = 3148,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7506,
    x = 2589,
    z = 3174,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2589,
    z = 3193,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7510,
    x = 2590,
    z = 3148,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7528,
    x = 2591,
    z = 3139,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2592,
    z = 3182,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2592,
    z = 3185,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2593,
    z = 3179,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7512,
    x = 2594,
    z = 3148,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7508,
    x = 2594,
    z = 3174,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2594,
    z = 3176,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7528,
    x = 2595,
    z = 3139,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7513,
    x = 2595,
    z = 3148,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7505,
    x = 2595,
    z = 3174,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2596,
    z = 3184,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2598,
    z = 3182,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7507,
    x = 2599,
    z = 3174,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_255,
    x = 2599,
    z = 3193,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(npc = Npcs.KHAZARD_GUARD_7509, x = 2600, z = 3174, height = 0, direction = Direction.SOUTH, static = true) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7506,
    x = 2601,
    z = 3174,
    height = 0,
    walkRadius = 0,
    direction = Direction.SOUTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2606,
    z = 3171,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2607,
    z = 3183,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_255,
    x = 2607,
    z = 3185,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2608,
    z = 3186,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2609,
    z = 3149,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_256,
    x = 2610,
    z = 3149,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7528,
    x = 2614,
    z = 3150,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_255,
    x = 2614,
    z = 3186,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD,
    x = 2616,
    z = 3172,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7528,
    x = 2618,
    z = 3159,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_7528,
    x = 2618,
    z = 3166,
    height = 0,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD_254,
    x = 2592,
    z = 3163,
    height = 3,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_GUARD,
    x = 2594,
    z = 3162,
    height = 3,
    walkRadius = 0,
    direction = Direction.NORTH,
    static = true,
) // Khazard Guard
spawn_npc(
    npc = Npcs.KHAZARD_LAUNDERER,
    x = 2564,
    z = 3172,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = true,
) // Khazard launderer
spawn_npc(npc = Npcs.LOCAL, x = 2583, z = 3171, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Local
spawn_npc(
    npc = Npcs.SLAVE_FIGHTER,
    x = 2595,
    z = 3142,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Slave fighter
spawn_npc(
    npc = Npcs.SLAVE_FIGHTER,
    x = 2616,
    z = 3160,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Slave fighter
spawn_npc(
    npc = Npcs.SLAVE_FIGHTER,
    x = 2616,
    z = 3163,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Slave fighter

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.DARK_KEBBIT,
    x = 2375,
    z = 3591,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark kebbit
spawn_npc(
    npc = Npcs.DARK_KEBBIT,
    x = 2379,
    z = 3588,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark kebbit
spawn_npc(
    npc = Npcs.DARK_KEBBIT,
    x = 2380,
    z = 3600,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark kebbit
spawn_npc(
    npc = Npcs.DARK_KEBBIT,
    x = 2383,
    z = 3595,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark kebbit
spawn_npc(
    npc = Npcs.DARK_KEBBIT,
    x = 2388,
    z = 3590,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark kebbit
spawn_npc(
    npc = Npcs.DARK_KEBBIT,
    x = 2389,
    z = 3598,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark kebbit
spawn_npc(
    npc = Npcs.DASHING_KEBBIT,
    x = 2375,
    z = 3589,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dashing kebbit
spawn_npc(
    npc = Npcs.DASHING_KEBBIT,
    x = 2385,
    z = 3596,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dashing kebbit
spawn_npc(
    npc = Npcs.DASHING_KEBBIT,
    x = 2387,
    z = 3589,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dashing kebbit
spawn_npc(
    npc = Npcs.MATTHIAS,
    x = 2376,
    z = 3606,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Matthias
spawn_npc(
    npc = Npcs.SPOTTED_KEBBIT,
    x = 2370,
    z = 3587,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spotted kebbit
spawn_npc(
    npc = Npcs.SPOTTED_KEBBIT,
    x = 2370,
    z = 3591,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spotted kebbit
spawn_npc(
    npc = Npcs.SPOTTED_KEBBIT,
    x = 2372,
    z = 3584,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spotted kebbit
spawn_npc(
    npc = Npcs.SPOTTED_KEBBIT,
    x = 2379,
    z = 3585,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spotted kebbit
spawn_npc(
    npc = Npcs.SPOTTED_KEBBIT,
    x = 2382,
    z = 3584,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spotted kebbit
spawn_npc(
    npc = Npcs.SPOTTED_KEBBIT,
    x = 2384,
    z = 3586,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spotted kebbit
spawn_npc(
    npc = Npcs.SPOTTED_KEBBIT,
    x = 2391,
    z = 3592,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spotted kebbit

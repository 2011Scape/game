package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = Npcs.ALECK, x = 2568, z = 3082, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Aleck
spawn_npc(
    npc = Npcs.BANKER_CLASSIC_FEMALE_GREY_495,
    x = 2615,
    z = 3091,
    height = 0,
    direction = Direction.WEST,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.BANKER_CLASSIC_MALE_GREY_494,
    x = 2615,
    z = 3092,
    height = 0,
    direction = Direction.WEST,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.BANKER_CLASSIC_MALE_GREY_494,
    x = 2615,
    z = 3094,
    height = 0,
    direction = Direction.WEST,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.BULLDOG,
    x = 2573,
    z = 3082,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bulldog
spawn_npc(npc = Npcs.COW, x = 2580, z = 3117, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Cow
spawn_npc(npc = Npcs.COW, x = 2582, z = 3122, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Cow
spawn_npc(npc = Npcs.COW, x = 2588, z = 3119, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Cow
spawn_npc(npc = Npcs.COW, x = 2604, z = 3114, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Cow
spawn_npc(
    npc = Npcs.DALMATIAN,
    x = 2574,
    z = 3082,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dalmatian
spawn_npc(
    npc = Npcs.FRENITA,
    x = 2566,
    z = 3098,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Frenita
spawn_npc(npc = Npcs.IMP, x = 2595, z = 3131, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Imp
spawn_npc(npc = Npcs.IMP, x = 2597, z = 3120, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Imp
spawn_npc(npc = Npcs.IMP, x = 2606, z = 3126, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Imp
spawn_npc(npc = Npcs.IMP, x = 2608, z = 3134, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Imp
spawn_npc(npc = Npcs.IMP, x = 2614, z = 3116, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Imp
spawn_npc(npc = Npcs.IMP, x = 2616, z = 3126, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Imp
spawn_npc(npc = Npcs.IMP, x = 2621, z = 3133, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Imp
spawn_npc(npc = Npcs.IMP, x = 2622, z = 3118, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Imp
spawn_npc(npc = Npcs.LEON, x = 2567, z = 3085, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Leon
spawn_npc(
    npc = Npcs.MAGIC_STORE_OWNER,
    x = 2595,
    z = 3087,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Magic Store owner
spawn_npc(npc = Npcs.MAN, x = 2596, z = 3104, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Man
spawn_npc(npc = Npcs.MAN_3, x = 2596, z = 3106, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Man
spawn_npc(
    npc = Npcs.PET_SHOP_OWNER,
    x = 2576,
    z = 3078,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pet shop owner
spawn_npc(
    npc = Npcs.PROFESSOR_IMBLEWYN,
    x = 2590,
    z = 3092,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Professor Imblewyn
spawn_npc(
    npc = Npcs.ROBE_STORE_OWNER,
    x = 2595,
    z = 3088,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Robe Store owner
spawn_npc(
    npc = Npcs.SELENA,
    x = 2578,
    z = 3101,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Selena
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2567,
    z = 3109,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2572,
    z = 3109,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2595,
    z = 3073,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2600,
    z = 3109,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2609,
    z = 3073,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2610,
    z = 3107,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2615,
    z = 3073,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2618,
    z = 3099,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2620,
    z = 3077,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.SOLDIER,
    x = 2620,
    z = 3095,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Soldier
spawn_npc(
    npc = Npcs.TOOL_LEPRECHAUN_7561,
    x = 2573,
    z = 3107,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tool leprechaun
spawn_npc(
    npc = Npcs.TRAMP_2794,
    x = 2606,
    z = 3100,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tramp
spawn_npc(
    npc = Npcs.WIZARD,
    x = 2588,
    z = 3090,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD,
    x = 2589,
    z = 3084,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD,
    x = 2586,
    z = 3089,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD,
    x = 2593,
    z = 3084,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD,
    x = 2595,
    z = 3090,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD,
    x = 2586,
    z = 3088,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD,
    x = 2589,
    z = 3085,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD,
    x = 2593,
    z = 3090,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD_DISTENTOR,
    x = 2594,
    z = 3089,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard Distentor
spawn_npc(
    npc = Npcs.ZAVISTIC_RARVE,
    x = 2588,
    z = 3091,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zavistic Rarve
spawn_npc(npc = Npcs.MAGIC_STORE_OWNER, x = 2587, z = 3085, walkRadius = 5, direction = Direction.NORTH_WEST) // Magic Store Owner

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2658,
    z = 9812,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat added by jazneta
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2662,
    z = 9806,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat added by jazneta
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2668,
    z = 9809,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_93,
    x = 2665,
    z = 9819,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2657,
    z = 9824,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2661,
    z = 9829,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2657,
    z = 9833,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SKELETON_93,
    x = 2668,
    z = 9828,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by jazneta
spawn_npc(
    npc = Npcs.SCORPION,
    x = 2645,
    z = 9823,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scorpion added by jazneta
spawn_npc(
    npc = Npcs.SCORPION,
    x = 2642,
    z = 9818,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scorpion added by jazneta
spawn_npc(
    npc = Npcs.SCORPION,
    x = 2642,
    z = 9812,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Scorpion added by jazneta

package gg.rsmod.plugins.content.areas.spawns

spawn_item(item = Items.BREWIN_GUIDE, amount = 1, x = 3823, z = 3001, respawnCycles = 90)
spawn_item(item = Items.BREWIN_GUIDE, amount = 1, x = 3818, z = 2998, respawnCycles = 90)

spawn_npc(
    npc = Npcs.MONKEY_4363,
    x = 3785,
    z = 2974,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.MONKEY_4363,
    x = 3806,
    z = 2975,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.MONKEY_4363,
    x = 3810,
    z = 2972,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.MONKEY_4363,
    x = 3812,
    z = 2976,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.MONKEY_4363,
    x = 3832,
    z = 2974,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.PARROT,
    x = 3822,
    z = 2954,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Parrot
spawn_npc(
    npc = Npcs.PARROT_4374,
    x = 3822,
    z = 3005,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Parrot
spawn_npc(
    npc = Npcs.PIRATE_IMPLING_7867,
    x = 3786,
    z = 2975,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate impling
spawn_npc(
    npc = Npcs.PIRATE_IMPLING_7867,
    x = 3809,
    z = 2977,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate impling
spawn_npc(
    npc = Npcs.PIRATE_IMPLING_7867,
    x = 3835,
    z = 2976,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate impling

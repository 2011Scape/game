package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.PEASANT_215,
    x = 2660,
    z = 4722,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Peasant added by jazneta
spawn_npc(
    npc = Npcs.PEASANT_215,
    x = 2650,
    z = 4719,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Peasant added by jazneta
spawn_npc(
    npc = Npcs.PEASANT_215,
    x = 2639,
    z = 4725,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Peasant added by jazneta
spawn_npc(
    npc = Npcs.RAM_5168,
    x = 2655,
    z = 4716,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5169,
    x = 2658,
    z = 4715,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5170,
    x = 2656,
    z = 4713,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5168,
    x = 2652,
    z = 4716,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5169,
    x = 2651,
    z = 4712,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5170,
    x = 2652,
    z = 4709,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5168,
    x = 2656,
    z = 4709,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5169,
    x = 2659,
    z = 4708,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5170,
    x = 2659,
    z = 4706,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5168,
    x = 2656,
    z = 4706,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5169,
    x = 2653,
    z = 4705,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5170,
    x = 2657,
    z = 4719,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.RAM_5168,
    x = 2660,
    z = 4717,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram added by jazneta
spawn_npc(
    npc = Npcs.PEASANT_215,
    x = 2651,
    z = 4704,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Peasant added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2630,
    z = 4689,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2632,
    z = 4685,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2639,
    z = 4687,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2642,
    z = 4683,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2639,
    z = 4680,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2636,
    z = 4677,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.KING_PERCIVAL,
    x = 2635,
    z = 4683,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // King Percival added by jazneta

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 8951, x = 2352, z = 3172, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Arianwyn
spawn_npc(
    npc = Npcs.AMAETHWR,
    x = 2347,
    z = 3164,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Amaethwr
spawn_npc(npc = Npcs.ARVEL, x = 2335, z = 3177, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Arvel
spawn_npc(npc = Npcs.BANKER_ELF_MALE_2354, x = 2352, z = 3166, height = 0, direction = Direction.SOUTH, static = true) // Banker
spawn_npc(npc = Npcs.BANKER_ELF_FEMALE_2355, x = 2354, z = 3166, height = 0, direction = Direction.SOUTH, static = true) // Banker
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2322,
    z = 3168,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2323,
    z = 3172,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2323,
    z = 3175,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2327,
    z = 3171,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2339,
    z = 3169,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2340,
    z = 3161,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2342,
    z = 3181,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2345,
    z = 3165,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2345,
    z = 3168,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2350,
    z = 3157,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2334,
    z = 3167,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2336,
    z = 3169,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2345,
    z = 3183,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2347,
    z = 3153,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2347,
    z = 3181,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2349,
    z = 3151,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.DALLDAV,
    x = 2324,
    z = 3164,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dalldav
spawn_npc(
    npc = Npcs.ELF_WARRIOR_2360,
    x = 2328,
    z = 3172,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Elf warrior
spawn_npc(
    npc = Npcs.ELF_WARRIOR_2359,
    x = 2329,
    z = 3171,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Elf warrior
spawn_npc(
    npc = Npcs.ELF_WARRIOR_2361,
    x = 2345,
    z = 3175,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Elf warrior
spawn_npc(
    npc = Npcs.ELF_WARRIOR_2362,
    x = 2344,
    z = 3170,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Elf warrior
spawn_npc(npc = Npcs.EOIN, x = 2337, z = 3175, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Eoin
spawn_npc(npc = Npcs.EUDAV, x = 2334, z = 3182, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Eudav
spawn_npc(
    npc = Npcs.GETHIN,
    x = 2335,
    z = 3158,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gethin
spawn_npc(npc = Npcs.GOREU, x = 2337, z = 3159, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Goreu
spawn_npc(npc = Npcs.GWIR, x = 2352, z = 3179, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gwir
spawn_npc(npc = Npcs.IONA, x = 2335, z = 3169, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Iona
spawn_npc(
    npc = Npcs.MAWRTH,
    x = 2331,
    z = 3165,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mawrth
spawn_npc(
    npc = Npcs.ORONWEN,
    x = 2324,
    z = 3179,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Oronwen
spawn_npc(
    npc = Npcs.RAM_3673,
    x = 2322,
    z = 3189,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ram
spawn_npc(
    npc = Npcs.SHEEP_43,
    x = 2319,
    z = 3191,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sheep
spawn_npc(
    npc = Npcs.SHEEP_1765,
    x = 2322,
    z = 3192,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sheep
spawn_npc(
    npc = Npcs.SHEEP_5158,
    x = 2325,
    z = 3190,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sheep
spawn_npc(
    npc = Npcs.TECLYN,
    x = 2344,
    z = 3163,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Teclyn
spawn_npc(
    npc = Npcs.YSGAWYN,
    x = 2343,
    z = 3169,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ysgawyn

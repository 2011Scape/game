package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CORPOREAL_BEAST,
    x = 2986,
    z = 4383,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Corporeal Beast

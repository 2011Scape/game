package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3553,
    z = 9675,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3555,
    z = 9674,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3553,
    z = 9674,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3553,
    z = 9679,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3553,
    z = 9681,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3551,
    z = 9683,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3551,
    z = 9682,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3534,
    z = 9719,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3560,
    z = 9677,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3545,
    z = 9677,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3538,
    z = 9691,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3532,
    z = 9694,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3536,
    z = 9694,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3533,
    z = 9695,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3536,
    z = 9698,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3537,
    z = 9697,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3531,
    z = 9711,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3568,
    z = 9704,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3570,
    z = 9698,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.CRYPT_RAT,
    x = 3555,
    z = 9679,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt rat
spawn_npc(
    npc = Npcs.GIANT_CRYPT_RAT,
    x = 3537,
    z = 9693,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt rat
spawn_npc(
    npc = Npcs.GIANT_CRYPT_RAT,
    x = 3537,
    z = 9697,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt rat
spawn_npc(
    npc = Npcs.CRYPT_SPIDER,
    x = 3567,
    z = 9690,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt spider
spawn_npc(
    npc = Npcs.CRYPT_SPIDER,
    x = 3570,
    z = 9695,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt spider
spawn_npc(
    npc = Npcs.CRYPT_SPIDER,
    x = 3569,
    z = 9694,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt spider
spawn_npc(
    npc = Npcs.CRYPT_SPIDER,
    x = 3565,
    z = 9690,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt spider
spawn_npc(
    npc = Npcs.CRYPT_SPIDER,
    x = 3565,
    z = 9697,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crypt spider
spawn_npc(
    npc = Npcs.GIANT_CRYPT_SPIDER,
    x = 3567,
    z = 9693,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt spider
spawn_npc(
    npc = Npcs.GIANT_CRYPT_SPIDER,
    x = 3571,
    z = 9692,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt spider
spawn_npc(
    npc = Npcs.GIANT_CRYPT_SPIDER,
    x = 3570,
    z = 9690,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt spider
spawn_npc(
    npc = Npcs.GIANT_CRYPT_SPIDER,
    x = 3570,
    z = 9693,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt spider
spawn_npc(
    npc = Npcs.GIANT_CRYPT_SPIDER,
    x = 3571,
    z = 9694,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt spider
spawn_npc(
    npc = Npcs.GIANT_CRYPT_SPIDER,
    x = 3570,
    z = 9698,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt spider
spawn_npc(
    npc = Npcs.SKELETON_2036,
    x = 3549,
    z = 9715,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_2036,
    x = 3552,
    z = 9710,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_2036,
    x = 3568,
    z = 9716,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_2036,
    x = 3568,
    z = 9686,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_2036,
    x = 3567,
    z = 9681,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_2037,
    x = 3552,
    z = 9709,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_2037,
    x = 3552,
    z = 9711,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.GIANT_CRYPT_RAT_4920,
    x = 3534,
    z = 9694,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt rat
spawn_npc(
    npc = Npcs.GIANT_CRYPT_RAT_4921,
    x = 3535,
    z = 9692,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant crypt rat
spawn_npc(
    npc = Npcs.SKELETON_5381,
    x = 3548,
    z = 9712,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5422,
    x = 3549,
    z = 9709,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton

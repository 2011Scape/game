package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.SHADOW_SPIDER,
    x = 2574,
    z = 9874,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shadow spider
spawn_npc(
    npc = Npcs.SHADOW_SPIDER,
    x = 2576,
    z = 9877,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shadow spider
spawn_npc(
    npc = Npcs.SHADOW_SPIDER,
    x = 2578,
    z = 9876,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shadow spider
spawn_npc(
    npc = Npcs.SHADOW_SPIDER,
    x = 2573,
    z = 9877,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shadow spider
spawn_npc(
    npc = Npcs.SKELETON_MAGE,
    x = 2587,
    z = 9880,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE,
    x = 2586,
    z = 9884,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE,
    x = 2590,
    z = 9884,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE,
    x = 2590,
    z = 9882,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.FIRE_GIANT,
    x = 2565,
    z = 9885,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fire giant
spawn_npc(
    npc = Npcs.FIRE_GIANT,
    x = 2563,
    z = 9886,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fire giant
spawn_npc(
    npc = Npcs.FIRE_GIANT,
    x = 2565,
    z = 9889,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fire giant
spawn_npc(
    npc = Npcs.FIRE_GIANT,
    x = 2569,
    z = 9887,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fire giant

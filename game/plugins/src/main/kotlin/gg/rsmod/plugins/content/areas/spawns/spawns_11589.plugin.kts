package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.DAGANNOTH_SUPREME,
    x = 2906,
    z = 4441,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth Supreme
spawn_npc(
    npc = Npcs.DAGANNOTH_PRIME,
    x = 2910,
    z = 4458,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth Prime
spawn_npc(
    npc = Npcs.DAGANNOTH_REX,
    x = 2924,
    z = 4441,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth Rex
spawn_npc(
    npc = Npcs.SPINOLYP,
    x = 2929,
    z = 4455,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP,
    x = 2913,
    z = 4466,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP,
    x = 2900,
    z = 4458,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP,
    x = 2897,
    z = 4443,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP,
    x = 2901,
    z = 4436,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP,
    x = 2907,
    z = 4434,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP,
    x = 2924,
    z = 4435,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP,
    x = 2930,
    z = 4442,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP_2894,
    x = 2930,
    z = 4449,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP_2894,
    x = 2925,
    z = 4461,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP_2894,
    x = 2908,
    z = 4465,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP_2894,
    x = 2898,
    z = 4452,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP_2894,
    x = 2899,
    z = 4439,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin
spawn_npc(
    npc = Npcs.SPINOLYP_2894,
    x = 2915,
    z = 4432,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spinolyp added by devin

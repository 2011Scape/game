package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 4986, x = 3304, z = 3113, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Irena
spawn_npc(npc = 4987, x = 3305, z = 3113, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Irena
spawn_npc(npc = 5577, x = 3291, z = 3104, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Scout
spawn_npc(npc = 6077, x = 3307, z = 3113, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ana
spawn_npc(
    npc = Npcs.DESERT_WOLF_6051,
    x = 3283,
    z = 3084,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Desert wolf
spawn_npc(
    npc = Npcs.DESERT_WOLF,
    x = 3283,
    z = 3108,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Desert wolf
spawn_npc(
    npc = Npcs.DESERT_WOLF_6050,
    x = 3310,
    z = 3076,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Desert wolf
spawn_npc(
    npc = Npcs.DESERT_WOLF,
    x = 3323,
    z = 3094,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Desert wolf
spawn_npc(
    npc = Npcs.MONKEY_2301,
    x = 3311,
    z = 3108,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Monkey
spawn_npc(
    npc = Npcs.MUSICIAN_8708,
    x = 3309,
    z = 3126,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Musician
spawn_npc(
    npc = Npcs.RUG_MERCHANT,
    x = 3311,
    z = 3109,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rug merchant
spawn_npc(
    npc = Npcs.SHANTAY,
    x = 3304,
    z = 3118,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shantay
spawn_npc(
    npc = Npcs.SHANTAY_GUARD,
    x = 3305,
    z = 3129,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shantay guard
spawn_npc(
    npc = Npcs.SHANTAY_GUARD,
    x = 3307,
    z = 3123,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shantay guard
spawn_npc(
    npc = Npcs.UGTHANKI,
    x = 3267,
    z = 3077,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ugthanki
spawn_npc(
    npc = Npcs.UGTHANKI,
    x = 3269,
    z = 3110,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ugthanki
spawn_npc(
    npc = Npcs.UGTHANKI,
    x = 3275,
    z = 3094,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ugthanki
spawn_npc(
    npc = Npcs.UGTHANKI,
    x = 3291,
    z = 3078,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ugthanki
spawn_npc(
    npc = Npcs.UGTHANKI,
    x = 3291,
    z = 3100,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ugthanki
spawn_npc(
    npc = Npcs.UGTHANKI,
    x = 3305,
    z = 3089,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ugthanki
spawn_npc(
    npc = Npcs.UGTHANKI,
    x = 3318,
    z = 3078,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ugthanki
spawn_npc(
    npc = Npcs.UGTHANKI,
    x = 3321,
    z = 3104,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ugthanki

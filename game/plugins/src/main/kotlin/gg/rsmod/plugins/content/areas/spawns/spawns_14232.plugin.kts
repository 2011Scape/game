package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BLOODVELD,
    x = 3572,
    z = 9738,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodveld
spawn_npc(
    npc = Npcs.BLOODVELD,
    x = 3570,
    z = 9735,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodveld
spawn_npc(
    npc = Npcs.BLOODVELD,
    x = 3573,
    z = 9742,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodveld
spawn_npc(
    npc = Npcs.BLOODVELD,
    x = 3563,
    z = 9736,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodveld
spawn_npc(
    npc = Npcs.BLOODVELD,
    x = 3566,
    z = 9737,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodveld

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CHICKEN,
    x = 2691,
    z = 3273,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1017,
    x = 2691,
    z = 3277,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1017,
    x = 2692,
    z = 3271,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN,
    x = 2695,
    z = 3274,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.HOLGART_700,
    x = 2720,
    z = 3306,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Holgart
spawn_npc(
    npc = Npcs.CAROLINE,
    x = 2715,
    z = 3300,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Caroline

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.KURASK,
    x = 3224,
    z = 4320,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kurask
spawn_npc(
    npc = Npcs.KURASK,
    x = 3224,
    z = 4320,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kurask
spawn_npc(
    npc = Npcs.KURASK_1609,
    x = 3224,
    z = 4320,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kurask
spawn_npc(
    npc = Npcs.KURASK_1609,
    x = 3224,
    z = 4320,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kurask
spawn_npc(
    npc = Npcs.ABERRANT_SPECTRE_7801,
    x = 3224,
    z = 4320,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Aberrant spectre
spawn_npc(
    npc = Npcs.ABERRANT_SPECTRE_7802,
    x = 3224,
    z = 4320,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Aberrant spectre
spawn_npc(
    npc = Npcs.ABERRANT_SPECTRE_7803,
    x = 3224,
    z = 4320,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Aberrant spectre
spawn_npc(
    npc = Npcs.ABERRANT_SPECTRE_7804,
    x = 3224,
    z = 4320,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Aberrant spectre

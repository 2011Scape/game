package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 824, x = 2484, z = 3487, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Daero
spawn_npc(npc = 4613, x = 2468, z = 3475, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Evil creature
spawn_npc(npc = 4616, x = 2466, z = 3496, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Evil creature
spawn_npc(npc = 5064, x = 2479, z = 3458, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Assistant Le Smith
spawn_npc(
    npc = Npcs.ALUFT_GIANNE_SNR,
    x = 2449,
    z = 3501,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Aluft Gianne snr.
spawn_npc(
    npc = Npcs.BARMAN,
    x = 2482,
    z = 3487,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barman
spawn_npc(
    npc = Npcs.BARMAN,
    x = 2482,
    z = 3489,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Barman
spawn_npc(
    npc = Npcs.BLURBERRY,
    x = 2480,
    z = 3490,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Blurberry
spawn_npc(
    npc = Npcs.BUTTERFLY_155,
    x = 2434,
    z = 3516,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY_156,
    x = 2444,
    z = 3491,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY_157,
    x = 2447,
    z = 3467,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY_157,
    x = 2448,
    z = 3469,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY_155,
    x = 2479,
    z = 3501,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.DALILA,
    x = 2448,
    z = 3500,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dalila
spawn_npc(
    npc = Npcs.GLOUGH,
    x = 2478,
    z = 3463,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glough
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2433,
    z = 3474,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2433,
    z = 3492,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2435, z = 3460, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2437,
    z = 3478,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2441, z = 3470, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2442,
    z = 3465,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2447, z = 3502, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2456,
    z = 3467,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2457, z = 3462, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2471,
    z = 3496,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2474,
    z = 3508,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2475,
    z = 3471,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2476,
    z = 3458,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2478, z = 3502, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2479,
    z = 3468,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2486,
    z = 3470,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2492,
    z = 3474,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2436,
    z = 3465,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2443,
    z = 3464,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2443,
    z = 3502,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2445,
    z = 3502,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2448,
    z = 3496,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2449,
    z = 3463,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2449, z = 3492, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2449,
    z = 3506,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2451, z = 3490, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2453,
    z = 3488,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2457,
    z = 3498,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2474, z = 3490, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2474,
    z = 3498,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2475,
    z = 3502,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2480,
    z = 3502,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2481,
    z = 3482,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2482, z = 3492, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(npc = Npcs.GNOME, x = 2482, z = 3498, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Gnome
spawn_npc(
    npc = Npcs.GNOME_67,
    x = 2490,
    z = 3503,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_68,
    x = 2465,
    z = 3489,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome
spawn_npc(
    npc = Npcs.GNOME_CHILD_160,
    x = 2474,
    z = 3500,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome child
spawn_npc(
    npc = Npcs.GNOME_CHILD,
    x = 2483,
    z = 3500,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome child
spawn_npc(
    npc = Npcs.GNOME_CHILD_161,
    x = 2484,
    z = 3501,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome child
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2441,
    z = 3497,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2443,
    z = 3489,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2447,
    z = 3510,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2450,
    z = 3485,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2451,
    z = 3507,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2460,
    z = 3487,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2461,
    z = 3509,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2464,
    z = 3465,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2464,
    z = 3468,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2464,
    z = 3472,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2468,
    z = 3465,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2468,
    z = 3468,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2468,
    z = 3472,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2472,
    z = 3484,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2475,
    z = 3502,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2477,
    z = 3489,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2477,
    z = 3512,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2478,
    z = 3497,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2481,
    z = 3485,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2482,
    z = 3499,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2464,
    z = 3496,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2467,
    z = 3495,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2473,
    z = 3495,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2448,
    z = 3495,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2448,
    z = 3497,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2460,
    z = 3486,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2463,
    z = 3480,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2465,
    z = 3497,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2465,
    z = 3510,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2466,
    z = 3480,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2467,
    z = 3494,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2467,
    z = 3510,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2471,
    z = 3496,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2483,
    z = 3495,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2483,
    z = 3497,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_TROOP_480,
    x = 2463,
    z = 3503,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome troop
spawn_npc(
    npc = Npcs.GNOME_TROOP,
    x = 2466,
    z = 3500,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome troop
spawn_npc(
    npc = Npcs.GNOME_WAITER,
    x = 2448,
    z = 3503,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome Waiter
spawn_npc(
    npc = Npcs.GNOME_WAITER,
    x = 2450,
    z = 3503,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome Waiter
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2438,
    z = 3465,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2442,
    z = 3505,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2448,
    z = 3486,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2448,
    z = 3489,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2449,
    z = 3487,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2450,
    z = 3488,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2450,
    z = 3489,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2450,
    z = 3505,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2454,
    z = 3465,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2463,
    z = 3508,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2470,
    z = 3488,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2474,
    z = 3457,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2479,
    z = 3503,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2486,
    z = 3467,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2437,
    z = 3463,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2448,
    z = 3489,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2449,
    z = 3486,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2457,
    z = 3488,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2476,
    z = 3488,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN_169,
    x = 2482,
    z = 3508,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2450,
    z = 3496,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2467,
    z = 3487,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2470,
    z = 3502,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GNOME_WOMAN,
    x = 2481,
    z = 3498,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome woman
spawn_npc(
    npc = Npcs.GULLUCK,
    x = 2468,
    z = 3486,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gulluck
spawn_npc(
    npc = Npcs.HECKEL_FUNCH,
    x = 2493,
    z = 3488,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Heckel Funch
spawn_npc(npc = Npcs.HUDO, x = 2448, z = 3509, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Hudo
spawn_npc(
    npc = Npcs.KING_NARNODE_SHAREEN,
    x = 2466,
    z = 3497,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // King Narnode Shareen
spawn_npc(
    npc = Npcs.MOUNTED_TERRORBIRD_GNOME,
    x = 2463,
    z = 3456,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mounted terrorbird gnome
spawn_npc(
    npc = Npcs.MOUNTED_TERRORBIRD_GNOME,
    x = 2468,
    z = 3456,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mounted terrorbird gnome
spawn_npc(
    npc = Npcs.PERRDUR,
    x = 2447,
    z = 3501,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Perrdur
spawn_npc(
    npc = Npcs.ROMETTI,
    x = 2482,
    z = 3510,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rometti
spawn_npc(npc = Npcs.SPANG, x = 2439, z = 3503, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Spang
spawn_npc(
    npc = Npcs.TAMBLE,
    x = 2442,
    z = 3503,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tamble
spawn_npc(
    npc = Npcs.TERRORCHICK,
    x = 2440,
    z = 3500,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Terrorchick
spawn_npc(
    npc = Npcs.TERRORCHICK,
    x = 2440,
    z = 3502,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Terrorchick
spawn_npc(
    npc = Npcs.TIMBLE,
    x = 2442,
    z = 3503,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Timble
spawn_npc(
    npc = Npcs.CAPTAIN_ERRDO,
    x = 2465,
    z = 3504,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Captain Errdo

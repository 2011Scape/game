package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.STRAVEN,
    x = 3246,
    z = 9781,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Straven, Quest NPC
spawn_npc(
    npc = Npcs.THIEF_5926,
    x = 3247,
    z = 9775,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Thieves to fill the room in phoenix hideout
spawn_npc(
    npc = Npcs.THIEF_5926,
    x = 3234,
    z = 9769,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Thieves to fill the room in phoenix hideout

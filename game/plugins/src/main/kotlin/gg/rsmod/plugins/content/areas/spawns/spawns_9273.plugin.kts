package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 3832, x = 2345, z = 3651, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Wise Old Man
spawn_npc(
    npc = Npcs.ARNOLD_LYDSPOR,
    x = 2330,
    z = 3691,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Arnold Lydspor
spawn_npc(
    npc = Npcs.DEVIN_MENDELBERG,
    x = 2313,
    z = 3686,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Devin Mendelberg
spawn_npc(npc = Npcs.FISHING_SPOT_3848, x = 2314, z = 3703, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_3848, x = 2326, z = 3700, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_3848, x = 2343, z = 3702, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_3848, x = 2349, z = 3702, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(
    npc = Npcs.FRANKLIN_CARANOS,
    x = 2344,
    z = 3667,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Franklin Caranos
spawn_npc(
    npc = Npcs.GEORGE_LAXMEISTER,
    x = 2323,
    z = 3684,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // George Laxmeister
spawn_npc(
    npc = Npcs.HERMAN_CARANOS,
    x = 2354,
    z = 3683,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Herman Caranos
spawn_npc(
    npc = Npcs.RAMARA_DU_CROISSANT,
    x = 2344,
    z = 3678,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ramara du Croissant
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2316,
    z = 3685,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2316,
    z = 3687,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2317,
    z = 3689,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2320,
    z = 3689,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2321,
    z = 3679,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2321,
    z = 3685,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2321,
    z = 3687,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2325,
    z = 3671,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2331,
    z = 3666,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2331,
    z = 3671,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage
spawn_npc(
    npc = Npcs.SKELETON_MAGE_3851,
    x = 2333,
    z = 3671,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton Mage

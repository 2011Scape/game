package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.MOSS_GIANT_1588,
    x = 2827,
    z = 3246,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT,
    x = 2831,
    z = 3243,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_1587,
    x = 2834,
    z = 3242,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2820,
    z = 3259,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2824,
    z = 3237,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5337,
    x = 2832,
    z = 3234,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5338,
    x = 2840,
    z = 3238,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5339,
    x = 2846,
    z = 3249,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5340,
    x = 2850,
    z = 3247,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 2850,
    z = 3256,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5338,
    x = 2852,
    z = 3260,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5337,
    x = 2854,
    z = 3249,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3240,
    z = 9992,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie
spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3240,
    z = 9994,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie
spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3242,
    z = 9994,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie
spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3242,
    z = 9992,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie
spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3244,
    z = 9995,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie
spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3242,
    z = 9996,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie
spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3240,
    z = 9997,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie
spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3243,
    z = 9998,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie
spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3241,
    z = 9999,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie
spawn_npc(
    npc = Npcs.ARMOURED_ZOMBIE,
    x = 3241,
    z = 10001,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Armoured zombie

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.DAGANNOTH,
    x = 2517,
    z = 10028,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH,
    x = 2525,
    z = 10028,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1339,
    x = 2527,
    z = 10023,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1339,
    x = 2523,
    z = 10020,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1340,
    x = 2521,
    z = 10024,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1340,
    x = 2517,
    z = 10020,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1341,
    x = 2518,
    z = 10017,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1341,
    x = 2522,
    z = 10014,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1342,
    x = 2527,
    z = 10018,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1342,
    x = 2532,
    z = 10024,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1343,
    x = 2526,
    z = 10027,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent
spawn_npc(
    npc = Npcs.DAGANNOTH_1343,
    x = 2523,
    z = 10020,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dagannoth added by trent

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.PIRATE_PETE,
    x = 3680,
    z = 3537,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate Pete
spawn_npc(
    npc = Npcs.TORTURED_SOUL,
    x = 3654,
    z = 3536,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tortured soul
spawn_npc(
    npc = Npcs.TORTURED_SOUL,
    x = 3657,
    z = 3534,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tortured soul
spawn_npc(
    npc = Npcs.TORTURED_SOUL,
    x = 3666,
    z = 3529,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tortured soul
spawn_npc(
    npc = Npcs.TORTURED_SOUL,
    x = 3668,
    z = 3537,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tortured soul
spawn_npc(
    npc = Npcs.TORTURED_SOUL,
    x = 3672,
    z = 3520,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tortured soul
spawn_npc(
    npc = Npcs.TORTURED_SOUL,
    x = 3673,
    z = 3528,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tortured soul

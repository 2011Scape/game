package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.PHRIN_SHADOW,
    x = 3501,
    z = 9724,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Phrin Shadow added by trent
spawn_npc(
    npc = Npcs.PHRIN_SHADOW,
    x = 3498,
    z = 9722,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Phrin Shadow added by trent
spawn_npc(
    npc = Npcs.PHRIN_SHADOW,
    x = 3493,
    z = 9723,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Phrin Shadow added by trent
spawn_npc(
    npc = Npcs.PHRIN_SHADOW,
    x = 3489,
    z = 9722,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Phrin Shadow added by trent
spawn_npc(
    npc = Npcs.PHRIN_SHADOW,
    x = 3487,
    z = 9724,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Phrin Shadow added by trent
spawn_npc(
    npc = Npcs.PHRIN_SHADOW,
    x = 3484,
    z = 9723,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Phrin Shadow added by trent
spawn_npc(
    npc = Npcs.PHRIN_SHADOW,
    x = 3482,
    z = 9725,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Phrin Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3516,
    z = 9707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3512,
    z = 9706,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3507,
    z = 9707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3504,
    z = 9704,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3502,
    z = 9707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3499,
    z = 9704,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3496,
    z = 9707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3492,
    z = 9705,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3490,
    z = 9708,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3483,
    z = 9704,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3482,
    z = 9707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3476,
    z = 9706,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3471,
    z = 9706,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3493,
    z = 9696,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3506,
    z = 9696,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.RIYL_SHADOW,
    x = 3471,
    z = 9695,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Riyl Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3492,
    z = 9684,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3495,
    z = 9681,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3492,
    z = 9677,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3493,
    z = 9673,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3492,
    z = 9669,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3496,
    z = 9667,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3502,
    z = 9669,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3508,
    z = 9669,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3514,
    z = 9669,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3517,
    z = 9667,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3516,
    z = 9675,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3516,
    z = 9680,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3516,
    z = 9685,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3516,
    z = 9689,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3517,
    z = 9694,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3507,
    z = 9686,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3504,
    z = 9688,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3481,
    z = 9687,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3472,
    z = 9688,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3468,
    z = 9685,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3469,
    z = 9678,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3469,
    z = 9674,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3469,
    z = 9670,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.ASYN_SHADOW,
    x = 3468,
    z = 9667,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Asyn Shadow added by trent
spawn_npc(
    npc = Npcs.FIYR_SHADOW,
    x = 3463,
    z = 9694,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fiyr Shadow added by trent
spawn_npc(
    npc = Npcs.FIYR_SHADOW,
    x = 3464,
    z = 9701,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fiyr Shadow added by trent
spawn_npc(
    npc = Npcs.FIYR_SHADOW,
    x = 3463,
    z = 9707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fiyr Shadow added by trent
spawn_npc(
    npc = Npcs.FIYR_SHADOW,
    x = 3463,
    z = 9714,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fiyr Shadow added by trent
spawn_npc(
    npc = Npcs.FIYR_SHADOW,
    x = 3463,
    z = 9719,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fiyr Shadow added by trent
spawn_npc(
    npc = Npcs.FIYR_SHADOW,
    x = 3463,
    z = 9725,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fiyr Shadow added by trent

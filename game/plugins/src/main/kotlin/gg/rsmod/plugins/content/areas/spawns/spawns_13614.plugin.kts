package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.DESERT_SNAKE,
    x = 3403,
    z = 2963,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Desert snake
spawn_npc(
    npc = Npcs.DESERT_SNAKE,
    x = 3429,
    z = 2976,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Desert snake
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3400,
    z = 2997,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3402,
    z = 2999,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3403,
    z = 2997,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3445,
    z = 2993,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3446,
    z = 2992,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3448,
    z = 2991,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3448,
    z = 2994,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal

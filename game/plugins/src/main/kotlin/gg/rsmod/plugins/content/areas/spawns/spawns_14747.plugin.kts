package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.METARIALUS,
    x = 3681,
    z = 9959,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Metarialus added by devin

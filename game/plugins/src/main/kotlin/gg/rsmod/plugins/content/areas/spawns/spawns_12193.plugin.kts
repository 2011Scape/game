package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.HILL_GIANT,
    x = 3044,
    z = 10321,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hill Giant
spawn_npc(
    npc = Npcs.HILL_GIANT,
    x = 3037,
    z = 10317,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hill Giant
spawn_npc(
    npc = Npcs.HILL_GIANT,
    x = 3044,
    z = 10316,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hill Giant
spawn_npc(
    npc = Npcs.HILL_GIANT,
    x = 3045,
    z = 10308,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hill Giant
spawn_npc(
    npc = Npcs.HILL_GIANT,
    x = 3048,
    z = 10339,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hill Giant
spawn_npc(
    npc = Npcs.HILL_GIANT,
    x = 3051,
    z = 10347,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hill Giant
spawn_npc(
    npc = Npcs.HILL_GIANT,
    x = 3048,
    z = 10317,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hill Giant
spawn_npc(
    npc = Npcs.CHAOS_DWARF,
    x = 3029,
    z = 10313,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF,
    x = 3028,
    z = 10309,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF,
    x = 3033,
    z = 10311,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF,
    x = 3035,
    z = 10309,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.CHAOS_DWARF,
    x = 3035,
    z = 10313,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos dwarf
spawn_npc(
    npc = Npcs.GIANT_SPIDER_4400,
    x = 3018,
    z = 10313,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant spider
spawn_npc(
    npc = Npcs.GIANT_SPIDER_4400,
    x = 3016,
    z = 10315,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant spider
spawn_npc(
    npc = Npcs.GIANT_SPIDER_4400,
    x = 3018,
    z = 10322,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant spider
spawn_npc(
    npc = Npcs.GIANT_SPIDER_4400,
    x = 3024,
    z = 10326,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant spider
spawn_npc(
    npc = Npcs.GIANT_SPIDER_4400,
    x = 3030,
    z = 10332,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant spider
spawn_npc(
    npc = Npcs.GIANT_SPIDER_4400,
    x = 3029,
    z = 10337,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant spider
spawn_npc(
    npc = Npcs.GIANT_SPIDER_4400,
    x = 3034,
    z = 10342,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant spider

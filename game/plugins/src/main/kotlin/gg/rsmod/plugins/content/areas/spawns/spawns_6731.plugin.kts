package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GRIM_REAPER,
    x = 1707,
    z = 4814,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grim Reaper added by trent
spawn_npc(
    npc = Npcs.MUNCHER,
    x = 1701,
    z = 4822,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Muncher added by trent

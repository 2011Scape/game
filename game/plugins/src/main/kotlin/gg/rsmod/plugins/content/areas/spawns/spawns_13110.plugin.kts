package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.JIGGLING_CRATE,
    x = 3298,
    z = 3514,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jiggling crate
spawn_npc(
    npc = Npcs.JIGGLING_CRATE,
    x = 3298,
    z = 3514,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jiggling crate
spawn_npc(
    npc = Npcs.JIGGLING_CRATE,
    x = 3304,
    z = 3506,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jiggling crate
spawn_npc(
    npc = Npcs.JIGGLING_CRATE,
    x = 3305,
    z = 3499,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jiggling crate
spawn_npc(
    npc = Npcs.JIGGLING_CRATE,
    x = 3307,
    z = 3506,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jiggling crate
spawn_npc(
    npc = Npcs.JIGGLING_CRATE,
    x = 3311,
    z = 3499,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jiggling crate
spawn_npc(npc = 759, x = 3306, z = 3513, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Fluffs
spawn_npc(npc = 5066, x = 3297, z = 3483, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Assistant Serf
spawn_npc(npc = 7009, x = 3282, z = 3467, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Surok Magis
spawn_npc(
    npc = Npcs.ANNA_JONES,
    x = 3285,
    z = 3468,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Anna Jones
spawn_npc(
    npc = Npcs.BARTENDER,
    x = 3277,
    z = 3488,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bartender
spawn_npc(npc = Npcs.BENCH, x = 3285, z = 3469, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Bench
spawn_npc(
    npc = Npcs.BLACK_KNIGHT,
    x = 3277,
    z = 3505,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Black Knight
spawn_npc(
    npc = Npcs.COOK_5911,
    x = 3285,
    z = 3489,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cook
spawn_npc(
    npc = Npcs.GUARDIAN,
    x = 3323,
    z = 3501,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guardian
spawn_npc(
    npc = Npcs.JOHNATHON,
    x = 3279,
    z = 3503,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Johnathon
spawn_npc(npc = Npcs.MAN_3, x = 3283, z = 3501, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Man
spawn_npc(
    npc = Npcs.MAN_5923,
    x = 3277,
    z = 3495,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Man
spawn_npc(
    npc = Npcs.OVERSEER,
    x = 3316,
    z = 3491,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Overseer
spawn_npc(npc = Npcs.PHIL, x = 3319, z = 3504, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Phil
spawn_npc(
    npc = Npcs.SAWMILL_OPERATOR,
    x = 3302,
    z = 3492,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sawmill operator
spawn_npc(npc = Npcs.THIEF, x = 3285, z = 3501, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Thief
spawn_npc(npc = Npcs.WILL, x = 3316, z = 3513, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Will
spawn_npc(
    npc = Npcs.WOMAN_BRUNETTE_PONYTAIL_5,
    x = 3278,
    z = 3502,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Woman
spawn_npc(
    npc = Npcs.WOMAN_BLOND_5924,
    x = 3279,
    z = 3496,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Woman

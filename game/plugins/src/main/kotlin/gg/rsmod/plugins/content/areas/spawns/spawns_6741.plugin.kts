package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.WIZARD_KORVAK,
    x = 1699,
    z = 5471,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard Korvak
spawn_npc(
    npc = Npcs.WIZARD_VIEF,
    x = 1698,
    z = 5474,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard Vief
spawn_npc(
    npc = Npcs.WIZARD_ACANTHA,
    x = 1693,
    z = 5473,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard Acantha
spawn_npc(
    npc = Npcs.WIZARD_ELRISS,
    x = 1694,
    z = 5462,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard Elriss
spawn_npc(
    npc = Npcs.WIZARD_8034,
    x = 1703,
    z = 5468,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD_8036,
    x = 1702,
    z = 5466,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD_8037,
    x = 1703,
    z = 5469,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD_8038,
    x = 1692,
    z = 5469,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD_8039,
    x = 1703,
    z = 5464,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.WIZARD_8040,
    x = 1691,
    z = 5473,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wizard
spawn_npc(
    npc = Npcs.LARRIAR,
    x = 1692,
    z = 5465,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Larriar

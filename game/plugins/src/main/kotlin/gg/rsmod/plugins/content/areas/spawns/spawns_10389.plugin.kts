package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.POISON_SPIDER,
    x = 2572,
    z = 9570,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Poison Spider
spawn_npc(
    npc = Npcs.POISON_SPIDER,
    x = 2578,
    z = 9578,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Poison Spider
spawn_npc(
    npc = Npcs.POISON_SPIDER,
    x = 2588,
    z = 9574,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Poison Spider
spawn_npc(
    npc = Npcs.POISON_SPIDER,
    x = 2598,
    z = 9568,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Poison Spider
spawn_npc(
    npc = Npcs.POISON_SPIDER,
    x = 2599,
    z = 9553,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Poison Spider

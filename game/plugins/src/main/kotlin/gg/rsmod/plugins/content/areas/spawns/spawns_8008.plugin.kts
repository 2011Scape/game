package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 2022,
    z = 4664,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 2028,
    z = 4664,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 2035,
    z = 4664,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 2032,
    z = 4661,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 2030,
    z = 4656,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 2023,
    z = 4656,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 2021,
    z = 4660,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 1995,
    z = 4661,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 1992,
    z = 4661,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 1988,
    z = 4662,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 1989,
    z = 4656,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 1993,
    z = 4653,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast
spawn_npc(
    npc = Npcs.DARK_BEAST,
    x = 1991,
    z = 4650,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dark beast

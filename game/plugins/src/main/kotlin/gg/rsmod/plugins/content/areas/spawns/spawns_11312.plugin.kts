package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BABY_GECKO_7285,
    x = 2833,
    z = 3106,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby gecko
spawn_npc(
    npc = Npcs.BABY_GECKO_7285,
    x = 2833,
    z = 3111,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby gecko
spawn_npc(
    npc = Npcs.BABY_GECKO_7285,
    x = 2837,
    z = 3103,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby gecko
spawn_npc(
    npc = Npcs.BABY_GECKO_7285,
    x = 2839,
    z = 3109,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby gecko
spawn_npc(
    npc = Npcs.BABY_GECKO_7285,
    x = 2842,
    z = 3105,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby gecko
spawn_npc(
    npc = Npcs.BABY_MONKEY_7229,
    x = 2832,
    z = 3088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby monkey
spawn_npc(
    npc = Npcs.BABY_MONKEY_7229,
    x = 2834,
    z = 3089,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby monkey
spawn_npc(
    npc = Npcs.BABY_MONKEY_7229,
    x = 2838,
    z = 3077,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby monkey
spawn_npc(
    npc = Npcs.BABY_MONKEY_7229,
    x = 2838,
    z = 3081,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby monkey
spawn_npc(
    npc = Npcs.BABY_MONKEY_7229,
    x = 2842,
    z = 3091,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby monkey
spawn_npc(
    npc = Npcs.BABY_MONKEY_7228,
    x = 2866,
    z = 3100,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby monkey
spawn_npc(
    npc = Npcs.BABY_MONKEY_7228,
    x = 2873,
    z = 3094,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Baby monkey
spawn_npc(
    npc = Npcs.GECKO_7290,
    x = 2845,
    z = 3086,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gecko
spawn_npc(
    npc = Npcs.GECKO_7290,
    x = 2851,
    z = 3083,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gecko
spawn_npc(npc = Npcs.GULL, x = 2818, z = 3133, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gull
spawn_npc(npc = Npcs.GULL, x = 2824, z = 3133, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gull
spawn_npc(npc = Npcs.GULL, x = 2829, z = 3131, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gull
spawn_npc(npc = Npcs.GULL, x = 2833, z = 3134, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gull
spawn_npc(
    npc = Npcs.HARPIE_BUG_SWARM,
    x = 2854,
    z = 3106,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Harpie Bug Swarm
spawn_npc(
    npc = Npcs.HARPIE_BUG_SWARM,
    x = 2859,
    z = 3106,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Harpie Bug Swarm
spawn_npc(
    npc = Npcs.HARPIE_BUG_SWARM,
    x = 2863,
    z = 3109,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Harpie Bug Swarm
spawn_npc(
    npc = Npcs.HARPIE_BUG_SWARM,
    x = 2863,
    z = 3115,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Harpie Bug Swarm
spawn_npc(
    npc = Npcs.HARPIE_BUG_SWARM,
    x = 2867,
    z = 3105,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Harpie Bug Swarm
spawn_npc(
    npc = Npcs.HARPIE_BUG_SWARM,
    x = 2870,
    z = 3112,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Harpie Bug Swarm
spawn_npc(
    npc = Npcs.HARPIE_BUG_SWARM,
    x = 2872,
    z = 3117,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Harpie Bug Swarm
spawn_npc(
    npc = Npcs.HARPIE_BUG_SWARM,
    x = 2873,
    z = 3108,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Harpie Bug Swarm
spawn_npc(
    npc = Npcs.HARPIE_BUG_SWARM,
    x = 2878,
    z = 3114,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Harpie Bug Swarm
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2855,
    z = 3085,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2855,
    z = 3089,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2857,
    z = 3085,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2858,
    z = 3083,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2858,
    z = 3092,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2861,
    z = 3082,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2861,
    z = 3088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2862,
    z = 3081,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2864,
    z = 3084,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2864,
    z = 3088,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2866,
    z = 3081,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2867,
    z = 3086,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2868,
    z = 3084,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(npc = Npcs.SNAKE, x = 2820, z = 3082, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Snake
spawn_npc(npc = Npcs.SNAKE, x = 2824, z = 3077, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Snake
spawn_npc(npc = Npcs.SNAKE, x = 2826, z = 3081, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Snake
spawn_npc(npc = Npcs.SNAKE, x = 2835, z = 3074, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Snake

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.OLD_CRONE,
    x = 3461,
    z = 3558,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Old crone

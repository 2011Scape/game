package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.ANKOU_4382,
    x = 3139,
    z = 3747,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ankou
spawn_npc(
    npc = Npcs.ANKOU_4382,
    x = 3141,
    z = 3737,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ankou
spawn_npc(
    npc = Npcs.ANKOU_4382,
    x = 3157,
    z = 3735,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ankou
spawn_npc(
    npc = Npcs.ANKOU_4382,
    x = 3164,
    z = 3727,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ankou
spawn_npc(
    npc = Npcs.ANKOU_4382,
    x = 3165,
    z = 3746,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ankou
spawn_npc(
    npc = Npcs.ANKOU_4382,
    x = 3172,
    z = 3713,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ankou
spawn_npc(
    npc = Npcs.ANKOU_4382,
    x = 3177,
    z = 3724,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ankou

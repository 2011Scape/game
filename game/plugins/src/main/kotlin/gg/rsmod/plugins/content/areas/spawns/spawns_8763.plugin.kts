package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BEDREAD_THE_BOLD,
    x = 2225,
    z = 3795,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bedread the bold
spawn_npc(
    npc = Npcs.BEEDYEYE_JONES,
    x = 2225,
    z = 3803,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Beedy-eye' Jones
spawn_npc(
    npc = Npcs.BEEFY_BURNS,
    x = 2222,
    z = 3789,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Beefy' Burns
spawn_npc(
    npc = Npcs.BETTY_BBOPPIN,
    x = 2222,
    z = 3804,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Betty' B.Boppin
spawn_npc(
    npc = Npcs.EAGLEEYE_SHULTZ,
    x = 2224,
    z = 3813,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Eagle-eye' Shultz
spawn_npc(
    npc = Npcs.FIRST_MATE_DAVEYBOY,
    x = 2225,
    z = 3788,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // First mate 'Davey-boy'
spawn_npc(
    npc = Npcs.JACK_SAILS,
    x = 2222,
    z = 3809,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jack Sails
spawn_npc(npc = Npcs.JAKE, x = 2205, z = 3807, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Jake
spawn_npc(
    npc = Npcs.JENNY_BLADE,
    x = 2227,
    z = 3797,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jenny Blade
spawn_npc(
    npc = Npcs.LECHEROUS_LEE,
    x = 2226,
    z = 3791,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lecherous' Lee
spawn_npc(
    npc = Npcs.LOKAR_SEARUNNER_4537,
    x = 2214,
    z = 3794,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lokar Searunner
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2195,
    z = 3808,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2196,
    z = 3822,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2197,
    z = 3805,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2197,
    z = 3813,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2201,
    z = 3812,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2202,
    z = 3824,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2203,
    z = 3804,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2207,
    z = 3813,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2211,
    z = 3818,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MOSS_GIANT_4706,
    x = 2214,
    z = 3819,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Moss giant
spawn_npc(
    npc = Npcs.MURKY_PAT,
    x = 2227,
    z = 3802,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Murky Pat
spawn_npc(
    npc = Npcs.PALMER,
    x = 2211,
    z = 3815,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Palmer
spawn_npc(
    npc = Npcs.PICARRON_PETE,
    x = 2223,
    z = 3799,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Picarron' Pete
spawn_npc(
    npc = Npcs.PIRATE,
    x = 2194,
    z = 3806,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(
    npc = Npcs.PIRATE_183,
    x = 2205,
    z = 3814,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Pirate
spawn_npc(npc = Npcs.RAT, x = 2223, z = 3793, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat
spawn_npc(npc = Npcs.RAT, x = 2226, z = 3800, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat
spawn_npc(
    npc = Npcs.STICKY_SANDERS,
    x = 2226,
    z = 3808,
    height = 3,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sticky' Sanders
spawn_npc(
    npc = Npcs.TOMMY_2TIMES,
    x = 2223,
    z = 3794,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tommy 2-times
spawn_npc(
    npc = Npcs.CAPTAIN_BENTLEY,
    x = 2223,
    z = 3797,
    height = 2,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Captain Bentley
spawn_npc(
    npc = Npcs.WILSON,
    x = 2199,
    z = 3808,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wilson

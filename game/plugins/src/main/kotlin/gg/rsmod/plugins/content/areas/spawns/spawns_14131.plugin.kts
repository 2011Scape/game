package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CYREG_PADDLEHORN,
    x = 3522,
    z = 3284,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cyreg Paddlehorn
spawn_npc(
    npc = Npcs.STRANGE_OLD_MAN,
    x = 3565,
    z = 3288,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Strange Old Man

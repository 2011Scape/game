package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BLOODWORM,
    x = 3520,
    z = 9616,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bloodworm

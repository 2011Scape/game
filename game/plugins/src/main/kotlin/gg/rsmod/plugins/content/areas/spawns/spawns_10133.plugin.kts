package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2535,
    z = 9555,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2531,
    z = 9555,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2543,
    z = 9559,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2544,
    z = 9565,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.SKELETON,
    x = 2540,
    z = 9565,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON,
    x = 2538,
    z = 9568,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON,
    x = 2542,
    z = 9568,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON,
    x = 2547,
    z = 9568,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.GOLRIE,
    x = 2515,
    z = 9580,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Golrie

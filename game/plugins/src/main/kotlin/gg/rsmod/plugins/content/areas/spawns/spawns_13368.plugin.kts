package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GULL_9717,
    x = 3348,
    z = 3612,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gull
spawn_npc(
    npc = Npcs.GULL_9717,
    x = 3351,
    z = 3631,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gull
spawn_npc(
    npc = Npcs.GULL_9717,
    x = 3380,
    z = 3629,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gull
spawn_npc(
    npc = Npcs.HILDA_13464,
    x = 3386,
    z = 3618,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hilda
spawn_npc(
    npc = Npcs.LOBSTER,
    x = 3386,
    z = 3627,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lobster
spawn_npc(
    npc = Npcs.LOBSTER,
    x = 3388,
    z = 3608,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lobster
spawn_npc(npc = Npcs.ROOK, x = 3382, z = 3617, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rook
spawn_npc(
    npc = Npcs.TORSTEG_13463,
    x = 3387,
    z = 3619,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Torsteg
spawn_npc(
    npc = Npcs.WATERFIEND,
    x = 3362,
    z = 3642,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Waterfiend
spawn_npc(
    npc = Npcs.WATERFIEND,
    x = 3363,
    z = 3647,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Waterfiend
spawn_npc(
    npc = Npcs.WATERFIEND,
    x = 3365,
    z = 3639,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Waterfiend
spawn_npc(
    npc = Npcs.WATERFIEND,
    x = 3365,
    z = 3643,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Waterfiend
spawn_npc(
    npc = Npcs.WATERFIEND,
    x = 3368,
    z = 3639,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Waterfiend
spawn_npc(
    npc = Npcs.WATERFIEND,
    x = 3368,
    z = 3645,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Waterfiend
spawn_npc(
    npc = Npcs.WATERFIEND,
    x = 3375,
    z = 3642,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Waterfiend
spawn_npc(
    npc = Npcs.WATERFIEND,
    x = 3376,
    z = 3646,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Waterfiend
spawn_npc(
    npc = Npcs.WATERFIEND,
    x = 3383,
    z = 3645,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Waterfiend

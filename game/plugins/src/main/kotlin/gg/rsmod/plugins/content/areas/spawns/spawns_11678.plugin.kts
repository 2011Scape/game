package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CART_CONDUCTOR_2183,
    x = 2906,
    z = 10173,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cart conductor
spawn_npc(
    npc = Npcs.CART_CONDUCTOR_2409,
    x = 2920,
    z = 10168,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cart conductor

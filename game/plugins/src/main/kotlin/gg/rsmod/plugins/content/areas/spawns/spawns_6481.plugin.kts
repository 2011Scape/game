package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.HELLHOUND,
    x = 1600,
    z = 5208,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hellhound
spawn_npc(
    npc = Npcs.IRON_DRAGON,
    x = 1640,
    z = 5232,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Iron dragon
spawn_npc(
    npc = Npcs.IRON_DRAGON,
    x = 1640,
    z = 5232,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Iron dragon

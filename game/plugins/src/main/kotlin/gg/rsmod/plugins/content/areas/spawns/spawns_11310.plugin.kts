package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.DURADEL_8466,
    x = 2869,
    z = 2982,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Duradel
spawn_npc(
    npc = Npcs.BANKER_CLASSIC_MALE_GREY_SPIKEYHAIR_499,
    x = 2851,
    z = 2954,
    height = 0,
    direction = Direction.NORTH,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.BANKER_CLASSIC_MALE_GREY_SPIKEYHAIR_499,
    x = 2853,
    z = 2955,
    height = 0,
    direction = Direction.NORTH,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.FERNAHEI,
    x = 2870,
    z = 2972,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fernahei
spawn_npc(npc = Npcs.FISHING_SPOT_317, x = 2855, z = 2974, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_317, x = 2855, z = 2977, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_317, x = 2856, z = 2974, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(
    npc = Npcs.KALEB_PARAMAYA,
    x = 2863,
    z = 2997,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Kaleb Paramaya
spawn_npc(npc = Npcs.OBLI, x = 2824, z = 2959, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Obli
spawn_npc(
    npc = Npcs.SERAVEL,
    x = 2870,
    z = 2974,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Seravel
spawn_npc(
    npc = Npcs.UNDEAD_ONE_505,
    x = 2870,
    z = 2954,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead one
spawn_npc(
    npc = Npcs.UNDEAD_ONE_504,
    x = 2871,
    z = 2951,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead one
spawn_npc(
    npc = Npcs.UNDEAD_ONE_5355,
    x = 2872,
    z = 2954,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead one
spawn_npc(
    npc = Npcs.UNDEAD_ONE_5358,
    x = 2873,
    z = 2951,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead one
spawn_npc(
    npc = Npcs.UNDEAD_ONE_505,
    x = 2876,
    z = 2947,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead one
spawn_npc(
    npc = Npcs.UNDEAD_ONE_5355,
    x = 2876,
    z = 2951,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead one
spawn_npc(
    npc = Npcs.UNDEAD_ONE_5356,
    x = 2876,
    z = 2955,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead one
spawn_npc(
    npc = Npcs.UNDEAD_ONE_504,
    x = 2876,
    z = 2957,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead one
spawn_npc(
    npc = Npcs.UNDEAD_ONE_5357,
    x = 2877,
    z = 2959,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead one
spawn_npc(
    npc = Npcs.VIGROY,
    x = 2834,
    z = 2954,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Vigroy
spawn_npc(
    npc = Npcs.YANNI_SALIKA,
    x = 2835,
    z = 2985,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Yanni Salika
spawn_npc(
    npc = Npcs.YOHNUS,
    x = 2857,
    z = 2963,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Yohnus

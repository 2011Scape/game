package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.EDMOND,
    x = 2571,
    z = 3333,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Edmond
spawn_npc(npc = 293, x = 2621, z = 3368, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Sick-looking sheep (2)
spawn_npc(npc = 293, x = 2622, z = 3366, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Sick-looking sheep (2)
spawn_npc(npc = 293, x = 2620, z = 3366, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Sick-looking sheep (2)
spawn_npc(npc = 295, x = 2610, z = 3385, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Sick-looking sheep (4)
spawn_npc(npc = 295, x = 2608, z = 3386, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Sick-looking sheep (4)
spawn_npc(npc = 295, x = 2607, z = 3384, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Sick-looking sheep (4)
spawn_npc(
    npc = Npcs.ALRENA,
    x = 2573,
    z = 3333,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Alrena
spawn_npc(
    npc = Npcs.BANKER_CLASSIC_MALE_GREY_494,
    x = 2614,
    z = 3330,
    height = 0,
    direction = Direction.NORTH,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.BANKER_CLASSIC_FEMALE_GREY_495,
    x = 2616,
    z = 3330,
    height = 0,
    direction = Direction.NORTH,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.BANKER_CLASSIC_FEMALE_GREY_495,
    x = 2619,
    z = 3330,
    height = 0,
    direction = Direction.NORTH,
    static = true,
) // Banker
spawn_npc(
    npc = Npcs.CHAOS_DRUID,
    x = 2561,
    z = 3355,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid
spawn_npc(
    npc = Npcs.CHAOS_DRUID,
    x = 2561,
    z = 3357,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid
spawn_npc(
    npc = Npcs.CHAOS_DRUID,
    x = 2563,
    z = 3355,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid
spawn_npc(
    npc = Npcs.CHAOS_DRUID,
    x = 2563,
    z = 3358,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid
spawn_npc(
    npc = Npcs.FARMER,
    x = 2617,
    z = 3355,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Farmer
spawn_npc(
    npc = Npcs.FARMER,
    x = 2621,
    z = 3355,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Farmer
spawn_npc(
    npc = Npcs.FARMER_BRUMTY,
    x = 2593,
    z = 3358,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Farmer Brumty
spawn_npc(npc = Npcs.FISHING_SPOT_311, x = 2561, z = 3374, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_311, x = 2566, z = 3370, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(
    npc = Npcs.GUARD_32,
    x = 2613,
    z = 3340,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard
spawn_npc(
    npc = Npcs.GUARD_32,
    x = 2617,
    z = 3338,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard
spawn_npc(
    npc = Npcs.GUARD_32,
    x = 2622,
    z = 3338,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard
spawn_npc(
    npc = Npcs.MASTER_FARMER,
    x = 2619,
    z = 3354,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Master Farmer
spawn_npc(
    npc = Npcs.MUSICIAN_8703,
    x = 2605,
    z = 3385,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Musician
spawn_npc(
    npc = Npcs.WARRIOR_WOMAN,
    x = 2569,
    z = 3387,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warrior woman
spawn_npc(
    npc = Npcs.WARRIOR_WOMAN,
    x = 2572,
    z = 3383,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warrior woman
spawn_npc(
    npc = Npcs.WARRIOR_WOMAN,
    x = 2572,
    z = 3388,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warrior woman
spawn_npc(
    npc = Npcs.WARRIOR_WOMAN,
    x = 2575,
    z = 3384,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Warrior woman

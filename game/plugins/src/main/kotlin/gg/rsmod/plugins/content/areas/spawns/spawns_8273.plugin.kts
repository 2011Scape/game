package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CATABLEPON_4399,
    x = 2080,
    z = 5240,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Catablepon
spawn_npc(
    npc = Npcs.CATABLEPON_4399,
    x = 2080,
    z = 5240,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Catablepon
spawn_npc(
    npc = Npcs.GIANT_SPIDER_4400,
    x = 2072,
    z = 5200,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant spider
spawn_npc(
    npc = Npcs.SPIDER_4401,
    x = 2072,
    z = 5200,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Spider

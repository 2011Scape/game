package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CAPTAIN_LAWGOF,
    x = 2567,
    z = 3460,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Captain Lawgof
spawn_npc(
    npc = Npcs.GALAHAD,
    x = 2612,
    z = 3474,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Galahad
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2571,
    z = 3481,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2572,
    z = 3472,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2578,
    z = 3476,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2586,
    z = 3473,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2591,
    z = 3479,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2592,
    z = 3484,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2594,
    z = 3474,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant bat
spawn_npc(
    npc = Npcs.GOBLIN_4261,
    x = 2564,
    z = 3456,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Goblin
spawn_npc(
    npc = Npcs.GUARD_4258,
    x = 2560,
    z = 3456,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard
spawn_npc(
    npc = Npcs.GUARD_4257,
    x = 2572,
    z = 3456,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard
spawn_npc(npc = Npcs.RAT, x = 2610, z = 3479, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat
spawn_npc(npc = Npcs.RAT, x = 2611, z = 3475, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat
spawn_npc(npc = Npcs.RAT, x = 2614, z = 3478, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat
spawn_npc(
    npc = Npcs.STANKERS,
    x = 2575,
    z = 3486,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Stankers

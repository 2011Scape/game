on_npc_option(Npcs.BOGROG, "swap") {
    openPouchTradeInInterface(player)
}

// Pouch tab
on_button(78, 24) {
    openPouchTradeInInterface(player)
}
// Scroll tab
on_button(78, 20) {
    openScrollTradeInInterface(player)
}

// Scroll trading
on_button(78, 14) {

}

// Pouch trading
on_button(78, 15) {

}

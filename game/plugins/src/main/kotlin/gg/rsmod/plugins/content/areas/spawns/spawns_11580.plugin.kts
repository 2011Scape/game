package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.UNDEAD_BROAV,
    x = 2887,
    z = 3870,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead broav
spawn_npc(
    npc = Npcs.UNDEAD_BROAV,
    x = 2891,
    z = 3884,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead broav
spawn_npc(
    npc = Npcs.UNDEAD_BROAV,
    x = 2915,
    z = 3867,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead broav
spawn_npc(
    npc = Npcs.UNDEAD_BROAV,
    x = 2919,
    z = 3860,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead broav
spawn_npc(
    npc = Npcs.UNDEAD_BROAV,
    x = 2921,
    z = 3878,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead broav

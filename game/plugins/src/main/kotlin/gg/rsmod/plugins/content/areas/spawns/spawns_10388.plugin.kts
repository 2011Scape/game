package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CHAOS_DRUID,
    x = 2610,
    z = 9485,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid
spawn_npc(
    npc = Npcs.CHAOS_DRUID,
    x = 2615,
    z = 9485,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid
spawn_npc(
    npc = Npcs.CHAOS_DRUID,
    x = 2613,
    z = 9517,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid
spawn_npc(
    npc = Npcs.CHAOS_DRUID,
    x = 2613,
    z = 9512,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid
spawn_npc(
    npc = Npcs.SIGBERT_THE_ADVENTURER,
    x = 2613,
    z = 9506,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sigbert the Adventurer
spawn_npc(
    npc = Npcs.SALARIN_THE_TWISTED,
    x = 2613,
    z = 9521,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Salarin the Twisted
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2601,
    z = 9485,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant Bat
spawn_npc(
    npc = Npcs.CHAOS_DRUID_WARRIOR,
    x = 2599,
    z = 9497,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid warrior
spawn_npc(
    npc = Npcs.CHAOS_DRUID_WARRIOR,
    x = 2587,
    z = 9499,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid warrior
spawn_npc(
    npc = Npcs.CHAOS_DRUID_WARRIOR,
    x = 2578,
    z = 9502,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid warrior
spawn_npc(
    npc = Npcs.CHAOS_DRUID_WARRIOR,
    x = 2579,
    z = 9508,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chaos druid warrior
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2577,
    z = 9524,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant Bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2568,
    z = 9528,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant Bat
spawn_npc(
    npc = Npcs.GIANT_BAT,
    x = 2574,
    z = 9531,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant Bat
spawn_npc(
    npc = Npcs.SKELETON,
    x = 2570,
    z = 9508,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON,
    x = 2565,
    z = 9507,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.ICE_WARRIOR,
    x = 2984,
    z = 9528,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ice warrior

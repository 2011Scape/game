package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 2002, x = 3315, z = 2849, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Wanderer
spawn_npc(npc = 5283, x = 3290, z = 2819, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Osman
spawn_npc(
    npc = Npcs.BILLY_GOAT_4935,
    x = 3324,
    z = 2841,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Billy Goat
spawn_npc(
    npc = Npcs.CROCODILE,
    x = 3276,
    z = 2828,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE,
    x = 3276,
    z = 2835,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE,
    x = 3276,
    z = 2870,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE,
    x = 3276,
    z = 2876,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE,
    x = 3279,
    z = 2839,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE,
    x = 3280,
    z = 2824,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE,
    x = 3284,
    z = 2837,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE,
    x = 3284,
    z = 2866,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.CROCODILE,
    x = 3292,
    z = 2845,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Crocodile
spawn_npc(
    npc = Npcs.GOAT_4934,
    x = 3325,
    z = 2843,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Goat
spawn_npc(
    npc = Npcs.GOAT_4934,
    x = 3325,
    z = 2845,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Goat
spawn_npc(
    npc = Npcs.GOAT_4933,
    x = 3326,
    z = 2847,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Goat
spawn_npc(
    npc = Npcs.GOAT_4933,
    x = 3327,
    z = 2844,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Goat
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3314,
    z = 2861,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3318,
    z = 2818,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3319,
    z = 2821,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3319,
    z = 2873,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3321,
    z = 2819,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3321,
    z = 2871,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3323,
    z = 2821,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3324,
    z = 2870,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.JACKAL,
    x = 3327,
    z = 2858,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jackal
spawn_npc(
    npc = Npcs.PRIEST_1989,
    x = 3294,
    z = 2820,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Priest
spawn_npc(
    npc = Npcs.PRIEST_1989,
    x = 3300,
    z = 2820,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Priest
spawn_npc(
    npc = Npcs.VULTURE,
    x = 3295,
    z = 2866,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Vulture
spawn_npc(
    npc = Npcs.VULTURE,
    x = 3297,
    z = 2863,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Vulture
spawn_npc(
    npc = Npcs.VULTURE,
    x = 3301,
    z = 2864,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Vulture

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BLACK_BEAR,
    x = 2606,
    z = 3008,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Black bear
spawn_npc(
    npc = Npcs.DWARF_118,
    x = 2596,
    z = 3056,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dwarf
spawn_npc(
    npc = Npcs.DWARF_118,
    x = 2600,
    z = 3062,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dwarf
spawn_npc(
    npc = Npcs.DWARF_118,
    x = 2605,
    z = 3057,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dwarf
spawn_npc(
    npc = Npcs.DWARF_118,
    x = 2608,
    z = 3061,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dwarf
spawn_npc(npc = Npcs.GORAD, x = 2577, z = 3021, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gorad
spawn_npc(
    npc = Npcs.OGRE_3419,
    x = 2561,
    z = 3020,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3419,
    x = 2567,
    z = 3046,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3419,
    x = 2571,
    z = 3027,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(npc = Npcs.OGRE, x = 2572, z = 3031, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Ogre
spawn_npc(
    npc = Npcs.OGRE_8658,
    x = 2575,
    z = 3024,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_3451,
    x = 2578,
    z = 3031,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre
spawn_npc(
    npc = Npcs.OGRE_COOK,
    x = 2563,
    z = 3009,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ogre cook
spawn_npc(npc = Npcs.TOBAN, x = 2576, z = 3027, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Toban
spawn_npc(
    npc = Npcs.UNICORN,
    x = 2570,
    z = 3058,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Unicorn
spawn_npc(
    npc = Npcs.UNICORN,
    x = 2579,
    z = 3066,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Unicorn

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.SKELETON_4385,
    x = 2304,
    z = 5160,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.GHOST_4387,
    x = 2304,
    z = 5160,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ghost
spawn_npc(
    npc = Npcs.SKELETON_5365,
    x = 2304,
    z = 5160,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.GHOST_5369,
    x = 2304,
    z = 5160,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ghost
spawn_npc(
    npc = Npcs.GHOST_5371,
    x = 2304,
    z = 5160,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ghost
spawn_npc(npc = Npcs.SHADE, x = 2304, z = 5160, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Shade
spawn_npc(npc = Npcs.SHADE, x = 2304, z = 5160, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Shade
spawn_npc(npc = Npcs.SHADE, x = 2304, z = 5160, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Shade
spawn_npc(npc = Npcs.SHADE, x = 2304, z = 5160, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Shade
spawn_npc(npc = Npcs.SHADE, x = 2304, z = 5160, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Shade
spawn_npc(npc = Npcs.SHADE, x = 2304, z = 5160, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Shade

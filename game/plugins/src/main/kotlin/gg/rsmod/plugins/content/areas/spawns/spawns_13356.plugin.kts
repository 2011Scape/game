package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 3124, x = 3366, z = 2845, height = 3, walkRadius = 5, direction = Direction.NORTH, static = false) // Pyramid block
spawn_npc(npc = 3125, x = 3372, z = 2847, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Pyramid block
spawn_npc(
    npc = Npcs.MUMMY_6753,
    x = 3381,
    z = 2816,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mummy
spawn_npc(
    npc = Npcs.MUMMY_6755,
    x = 3382,
    z = 2820,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mummy
spawn_npc(
    npc = Npcs.MUMMY_6756,
    x = 3384,
    z = 2822,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mummy
spawn_npc(
    npc = Npcs.MUMMY_6754,
    x = 3386,
    z = 2824,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mummy
spawn_npc(
    npc = Npcs.SIMON_TEMPLETON,
    x = 3349,
    z = 2827,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Simon Templeton
spawn_npc(
    npc = Npcs.TALENT_SCOUT_14363,
    x = 3350,
    z = 2831,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Talent scout
spawn_npc(
    npc = Npcs.VULTURE,
    x = 3333,
    z = 2864,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Vulture
spawn_npc(
    npc = Npcs.VULTURE,
    x = 3335,
    z = 2860,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Vulture
spawn_npc(
    npc = Npcs.VULTURE,
    x = 3338,
    z = 2864,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Vulture

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CORMORANT_9718,
    x = 3393,
    z = 3774,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Cormorant
spawn_npc(
    npc = Npcs.DUNGEONEERING_TUTOR,
    x = 3452,
    z = 3720,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Dungeoneering tutor
spawn_npc(
    npc = Npcs.FREMENNIK_BANKER,
    x = 3448,
    z = 3720,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fremennik banker

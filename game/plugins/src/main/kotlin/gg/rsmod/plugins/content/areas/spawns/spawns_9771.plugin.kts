package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GIANT_SPIDER_60,
    x = 2448,
    z = 2784,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Giant spider
spawn_npc(
    npc = Npcs.WIMPY_BIRD,
    x = 2488,
    z = 2768,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Wimpy bird

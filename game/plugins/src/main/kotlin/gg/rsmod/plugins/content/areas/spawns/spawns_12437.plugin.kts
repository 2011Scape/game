package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.CHICKEN,
    x = 3109,
    z = 9569,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.SEDRIDOR,
    x = 3105,
    z = 9569,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Sedridor
spawn_npc(
    npc = Npcs.ISIDOR,
    x = 3103,
    z = 9560,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Isidor

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.TORMENTED_DEMON,
    x = 2599,
    z = 5736,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tormented demon
spawn_npc(
    npc = Npcs.TORMENTED_DEMON,
    x = 2609,
    z = 5729,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tormented demon
spawn_npc(
    npc = Npcs.TORMENTED_DEMON,
    x = 2607,
    z = 5717,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tormented demon
spawn_npc(
    npc = Npcs.TORMENTED_DEMON,
    x = 2592,
    z = 5723,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tormented demon
spawn_npc(
    npc = Npcs.TORMENTED_DEMON,
    x = 2588,
    z = 5728,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tormented demon

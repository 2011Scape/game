package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BLACK_WARLOCK,
    x = 5534,
    z = 2203,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Black warlock
spawn_npc(
    npc = Npcs.BLACK_WARLOCK,
    x = 5548,
    z = 2197,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Black warlock
spawn_npc(
    npc = Npcs.BLACK_WARLOCK,
    x = 5558,
    z = 2209,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Black warlock
spawn_npc(
    npc = Npcs.GRENWALL,
    x = 5518,
    z = 2204,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grenwall
spawn_npc(
    npc = Npcs.GRENWALL,
    x = 5523,
    z = 2200,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grenwall
spawn_npc(
    npc = Npcs.GRENWALL,
    x = 5525,
    z = 2204,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grenwall
spawn_npc(
    npc = Npcs.GRENWALL,
    x = 5529,
    z = 2202,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grenwall

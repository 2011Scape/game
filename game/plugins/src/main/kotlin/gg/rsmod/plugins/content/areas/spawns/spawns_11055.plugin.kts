package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GABOOTY,
    x = 2785,
    z = 3066,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gabooty
spawn_npc(npc = 2484, x = 2782, z = 3058, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Tiadeche
spawn_npc(npc = 2486, x = 2789, z = 3055, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Tinsay
spawn_npc(npc = 2488, x = 2799, z = 3058, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Tamayu
spawn_npc(npc = 2519, x = 2794, z = 3064, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gabooty
spawn_npc(npc = 2525, x = 2800, z = 3048, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Jagbakoba
spawn_npc(
    npc = Npcs.CHICKEN,
    x = 2780,
    z = 3064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1017,
    x = 2788,
    z = 3061,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1017,
    x = 2805,
    z = 3064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN,
    x = 2808,
    z = 3067,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(npc = Npcs.FISHING_SPOT_1174, x = 2791, z = 3019, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_1174, x = 2801, z = 3010, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(npc = Npcs.FISHING_SPOT_1174, x = 2807, z = 3021, height = 0, direction = Direction.NORTH, static = true) // Fishing spot
spawn_npc(
    npc = Npcs.HOBGOBLIN_13100,
    x = 2787,
    z = 3013,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HOBGOBLIN_13100,
    x = 2791,
    z = 3012,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HOBGOBLIN_13100,
    x = 2794,
    z = 3012,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HORNED_GRAAHK,
    x = 2766,
    z = 3008,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Horned graahk
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2780,
    z = 3029,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.JUNGLE_SPIDER,
    x = 2780,
    z = 3031,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jungle spider
spawn_npc(
    npc = Npcs.TRIBESMAN,
    x = 2771,
    z = 3014,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tribesman
spawn_npc(
    npc = Npcs.TRIBESMAN,
    x = 2773,
    z = 3017,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tribesman
spawn_npc(
    npc = Npcs.TRIBESMAN,
    x = 2777,
    z = 3068,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tribesman
spawn_npc(
    npc = Npcs.TRIBESMAN,
    x = 2801,
    z = 3067,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tribesman

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = Npcs.ALICE, x = 3627, z = 3526, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Alice
spawn_npc(npc = Npcs.LYRA, x = 3601, z = 3527, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Lyra
spawn_npc(
    npc = Npcs.OLD_MAN,
    x = 3617,
    z = 3544,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Old man
spawn_npc(
    npc = Npcs.TOOL_LEPRECHAUN_7569,
    x = 3604,
    z = 3528,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Tool leprechaun
spawn_npc(
    npc = Npcs.UNDEAD_CHICKEN,
    x = 3627,
    z = 3531,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead chicken
spawn_npc(
    npc = Npcs.UNDEAD_CHICKEN,
    x = 3629,
    z = 3530,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead chicken
spawn_npc(
    npc = Npcs.UNDEAD_CHICKEN,
    x = 3631,
    z = 3529,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead chicken
spawn_npc(
    npc = Npcs.UNDEAD_CHICKEN,
    x = 3631,
    z = 3531,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead chicken
spawn_npc(
    npc = Npcs.UNDEAD_CHICKEN,
    x = 3632,
    z = 3526,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead chicken
spawn_npc(
    npc = Npcs.UNDEAD_CHICKEN,
    x = 3633,
    z = 3525,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead chicken
spawn_npc(
    npc = Npcs.UNDEAD_COW,
    x = 3611,
    z = 3527,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead cow
spawn_npc(
    npc = Npcs.UNDEAD_COW,
    x = 3616,
    z = 3525,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead cow
spawn_npc(
    npc = Npcs.UNDEAD_COW,
    x = 3620,
    z = 3524,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead cow
spawn_npc(
    npc = Npcs.UNDEAD_COW,
    x = 3622,
    z = 3527,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Undead cow

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 4625, x = 2458, z = 3383, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Evil creature
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2478,
    z = 3373,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2479,
    z = 3381,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2481,
    z = 3384,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(
    npc = Npcs.BUTTERFLY,
    x = 2490,
    z = 3371,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Butterfly
spawn_npc(npc = Npcs.FEMI, x = 2459, z = 3382, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Femi
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2459,
    z = 3385,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2460,
    z = 3382,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD_164,
    x = 2462,
    z = 3382,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.GNOME_GUARD,
    x = 2463,
    z = 3385,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gnome guard
spawn_npc(
    npc = Npcs.HOBGOBLIN_13246,
    x = 2443,
    z = 3359,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HOBGOBLIN_13246,
    x = 2445,
    z = 3359,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HOBGOBLIN_13100,
    x = 2445,
    z = 3362,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HOBGOBLIN_13246,
    x = 2446,
    z = 3357,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HOBGOBLIN_13100,
    x = 2447,
    z = 3361,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HOBGOBLIN_13246,
    x = 2448,
    z = 3329,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HOBGOBLIN_13246,
    x = 2448,
    z = 3331,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.HOBGOBLIN_13246,
    x = 2451,
    z = 3330,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Hobgoblin
spawn_npc(
    npc = Npcs.JORRAL,
    x = 2437,
    z = 3347,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Jorral
spawn_npc(
    npc = Npcs.MOUNTED_TERRORBIRD_GNOME,
    x = 2432,
    z = 3387,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mounted terrorbird gnome
spawn_npc(
    npc = Npcs.MOUNTED_TERRORBIRD_GNOME,
    x = 2442,
    z = 3388,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mounted terrorbird gnome
spawn_npc(
    npc = Npcs.MOUNTED_TERRORBIRD_GNOME,
    x = 2453,
    z = 3363,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mounted terrorbird gnome
spawn_npc(
    npc = Npcs.MOUNTED_TERRORBIRD_GNOME,
    x = 2463,
    z = 3375,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Mounted terrorbird gnome
spawn_npc(
    npc = Npcs.TICKET_VENDOR,
    x = 2443,
    z = 3380,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ticket vendor

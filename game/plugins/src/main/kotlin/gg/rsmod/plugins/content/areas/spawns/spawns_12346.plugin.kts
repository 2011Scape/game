package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.SKELETON_5367,
    x = 3092,
    z = 3717,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5368,
    x = 3096,
    z = 3738,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_4386,
    x = 3097,
    z = 3728,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5368,
    x = 3102,
    z = 3716,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5367,
    x = 3105,
    z = 3721,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5367,
    x = 3113,
    z = 3740,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5368,
    x = 3118,
    z = 3733,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_4386,
    x = 3128,
    z = 3719,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_5368,
    x = 3128,
    z = 3741,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton
spawn_npc(
    npc = Npcs.SKELETON_4386,
    x = 3130,
    z = 3754,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton

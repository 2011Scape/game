package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.FOREMAN,
    x = 2999,
    z = 3043,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Foreman
spawn_npc(
    npc = Npcs.GLO_CARANOCK,
    x = 2956,
    z = 3025,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // G.L.O. Caranock
spawn_npc(npc = Npcs.GULL, x = 2974, z = 3068, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gull
spawn_npc(npc = Npcs.GULL, x = 2977, z = 3052, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gull
spawn_npc(npc = Npcs.GULL, x = 2987, z = 3034, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gull
spawn_npc(npc = Npcs.GULL, x = 2990, z = 3025, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gull
spawn_npc(npc = Npcs.GULL, x = 2992, z = 3040, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gull
spawn_npc(npc = Npcs.SHARK, x = 3003, z = 3069, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Shark
spawn_npc(npc = Npcs.SHARK, x = 3007, z = 3063, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Shark
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_675,
    x = 2944,
    z = 3040,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER,
    x = 2953,
    z = 3056,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER,
    x = 2953,
    z = 3061,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_39,
    x = 2955,
    z = 3029,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER,
    x = 2957,
    z = 3032,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_39,
    x = 2957,
    z = 3055,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER,
    x = 2969,
    z = 3034,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_39,
    x = 2969,
    z = 3042,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_39,
    x = 2976,
    z = 3064,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER,
    x = 2983,
    z = 3049,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_39,
    x = 2983,
    z = 3061,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_39,
    x = 2987,
    z = 3049,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_39,
    x = 2988,
    z = 3058,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_39,
    x = 2995,
    z = 3061,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.SHIPYARD_WORKER_39,
    x = 3001,
    z = 3049,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Shipyard worker
spawn_npc(
    npc = Npcs.TRADER_CREWMEMBER_4652,
    x = 3001,
    z = 3033,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Trader Crewmember
spawn_npc(
    npc = Npcs.TRADER_CREWMEMBER_4654,
    x = 3001,
    z = 3034,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Trader Crewmember

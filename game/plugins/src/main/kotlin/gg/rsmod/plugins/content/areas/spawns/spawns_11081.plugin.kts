package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.BLACK_KNIGHT_TITAN,
    x = 2791,
    z = 4722,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Black Knight Titan added by jazneta
spawn_npc(
    npc = Npcs.PEASANT,
    x = 2788,
    z = 4722,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Peasant added by jazneta
spawn_npc(
    npc = Npcs.FISHERMAN,
    x = 2798,
    z = 4705,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fisherman added by jazneta
spawn_npc(
    npc = Npcs.PEASANT,
    x = 2787,
    z = 4698,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Peasant added by jazneta
spawn_npc(
    npc = Npcs.PEASANT,
    x = 2777,
    z = 4703,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Peasant added by jazneta
spawn_npc(
    npc = Npcs.PEASANT,
    x = 2773,
    z = 4724,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Peasant added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2764,
    z = 4691,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2758,
    z = 4688,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2768,
    z = 4685,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.GRAIL_MAIDEN,
    x = 2766,
    z = 4680,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Grail Maiden added by jazneta
spawn_npc(
    npc = Npcs.THE_FISHER_KING,
    x = 2764,
    z = 4684,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // The Fisher King added by jazneta

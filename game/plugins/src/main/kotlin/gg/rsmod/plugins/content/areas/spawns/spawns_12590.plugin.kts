package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 1922, x = 3185, z = 2982, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Eblis
spawn_npc(npc = 2384, x = 3161, z = 2983, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Mysterious ghost
spawn_npc(
    npc = Npcs.BANDIT_1928,
    x = 3157,
    z = 2986,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1927,
    x = 3157,
    z = 2989,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3158,
    z = 2977,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3158,
    z = 2987,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1929,
    x = 3159,
    z = 2977,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3159,
    z = 2979,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1930,
    x = 3161,
    z = 2977,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1930,
    x = 3161,
    z = 2981,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1930,
    x = 3161,
    z = 2982,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3161,
    z = 2983,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3161,
    z = 2986,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1928,
    x = 3162,
    z = 2987,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1927,
    x = 3162,
    z = 2989,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1930,
    x = 3163,
    z = 2977,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3163,
    z = 2980,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3163,
    z = 2982,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3163,
    z = 2986,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3164,
    z = 2978,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1926,
    x = 3164,
    z = 2984,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3165,
    z = 2960,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3168,
    z = 2989,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3171,
    z = 2975,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3173,
    z = 2972,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3173,
    z = 2981,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3175,
    z = 2974,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3180,
    z = 2992,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3183,
    z = 2974,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3183,
    z = 2975,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3185,
    z = 2985,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_1931,
    x = 3187,
    z = 2984,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit
spawn_npc(
    npc = Npcs.BANDIT_SHOPKEEPER,
    x = 3175,
    z = 2988,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bandit shopkeeper
spawn_npc(
    npc = Npcs.BARTENDER_1921,
    x = 3159,
    z = 2981,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Bartender

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 3829, x = 2367, z = 3490, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Kathy Corkat
spawn_npc(npc = 5125, x = 2316, z = 3501, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Nickolaus
spawn_npc(npc = Npcs.EAGLE, x = 2319, z = 3477, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Eagle
spawn_npc(npc = Npcs.EAGLE, x = 2323, z = 3485, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Eagle
spawn_npc(npc = Npcs.EAGLE, x = 2334, z = 3485, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Eagle
spawn_npc(npc = Npcs.EAGLE, x = 2334, z = 3495, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Eagle
spawn_npc(
    npc = Npcs.FERRET,
    x = 2305,
    z = 3516,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ferret
spawn_npc(
    npc = Npcs.FERRET,
    x = 2309,
    z = 3515,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ferret
spawn_npc(
    npc = Npcs.FERRET,
    x = 2311,
    z = 3506,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ferret
spawn_npc(
    npc = Npcs.FERRET,
    x = 2311,
    z = 3519,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ferret
spawn_npc(
    npc = Npcs.FERRET,
    x = 2313,
    z = 3510,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ferret
spawn_npc(
    npc = Npcs.FERRET,
    x = 2316,
    z = 3500,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ferret
spawn_npc(
    npc = Npcs.FERRET,
    x = 2316,
    z = 3511,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ferret
spawn_npc(
    npc = Npcs.FERRET,
    x = 2317,
    z = 3505,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ferret
spawn_npc(
    npc = Npcs.FERRET,
    x = 2319,
    z = 3518,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ferret
spawn_npc(
    npc = Npcs.MUSICIAN_8702,
    x = 2363,
    z = 3492,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Musician

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = 1375, x = 2499, z = 3857, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Advisor Ghrim added by trent
spawn_npc(npc = 1372, x = 2502, z = 3868, height = 1, walkRadius = 5, direction = Direction.NORTH, static = false) // Princess Astrid
spawn_npc(npc = Npcs.ALRIK, x = 2514, z = 3855, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Alrik
spawn_npc(
    npc = Npcs.BRODDI,
    x = 2512,
    z = 3871,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Broddi
spawn_npc(
    npc = Npcs.CHICKEN_1401,
    x = 2512,
    z = 3862,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1401,
    x = 2512,
    z = 3867,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1402,
    x = 2514,
    z = 3859,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1402,
    x = 2516,
    z = 3865,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1401,
    x = 2537,
    z = 3892,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1401,
    x = 2539,
    z = 3893,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1401,
    x = 2545,
    z = 3893,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.CHICKEN_1401,
    x = 2547,
    z = 3893,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Chicken
spawn_npc(
    npc = Npcs.DERRIK,
    x = 2551,
    z = 3897,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Derrik
spawn_npc(npc = Npcs.EINAR, x = 2501, z = 3868, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Einar
spawn_npc(
    npc = Npcs.FARMER_1377,
    x = 2538,
    z = 3852,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Farmer
spawn_npc(
    npc = Npcs.FISHMONGER_1393,
    x = 2518,
    z = 3866,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Fishmonger
spawn_npc(
    npc = Npcs.FLOWER_GIRL,
    x = 2514,
    z = 3866,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Flower Girl
spawn_npc(
    npc = Npcs.GARDENER_GUNNHILD,
    x = 2527,
    z = 3855,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Gardener Gunnhild
spawn_npc(
    npc = Npcs.GREENGROCER_1394,
    x = 2516,
    z = 3864,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Greengrocer
spawn_npc(
    npc = Npcs.GUARD_3941,
    x = 2509,
    z = 3848,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard
spawn_npc(
    npc = Npcs.GUARD_1374,
    x = 2505,
    z = 3856,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard
spawn_npc(
    npc = Npcs.GUARD_1374,
    x = 2505,
    z = 3864,
    height = 1,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard
spawn_npc(npc = Npcs.HALLA, x = 2541, z = 3896, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Halla
spawn_npc(
    npc = Npcs.LUMBERJACK_LEIF,
    x = 2550,
    z = 3869,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Lumberjack Leif
spawn_npc(
    npc = Npcs.MINER_MAGNUS,
    x = 2526,
    z = 3893,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Miner Magnus
spawn_npc(
    npc = Npcs.MUSICIAN_8716,
    x = 2536,
    z = 3861,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Musician
spawn_npc(
    npc = Npcs.PENGUIN,
    x = 2499,
    z = 3893,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Penguin
spawn_npc(
    npc = Npcs.PENGUIN,
    x = 2499,
    z = 3896,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Penguin
spawn_npc(
    npc = Npcs.PENGUIN,
    x = 2500,
    z = 3895,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Penguin
spawn_npc(
    npc = Npcs.PENGUIN,
    x = 2501,
    z = 3896,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Penguin
spawn_npc(
    npc = Npcs.PENGUIN,
    x = 2501,
    z = 3897,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Penguin
spawn_npc(
    npc = Npcs.PENGUIN,
    x = 2503,
    z = 3898,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Penguin
spawn_npc(
    npc = Npcs.PENGUIN,
    x = 2505,
    z = 3897,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Penguin
spawn_npc(
    npc = Npcs.PENGUIN,
    x = 2505,
    z = 3899,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Penguin
spawn_npc(
    npc = Npcs.RABBIT_5829,
    x = 2545,
    z = 3869,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rabbit
spawn_npc(
    npc = Npcs.RABBIT_1404,
    x = 2549,
    z = 3869,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rabbit
spawn_npc(
    npc = Npcs.RABBIT_1404,
    x = 2552,
    z = 3866,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rabbit
spawn_npc(
    npc = Npcs.RABBIT_5829,
    x = 2554,
    z = 3871,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rabbit
spawn_npc(
    npc = Npcs.RAGNAR,
    x = 2518,
    z = 3859,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ragnar
spawn_npc(
    npc = Npcs.RAGNVALD,
    x = 2518,
    z = 3867,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Ragnvald
spawn_npc(
    npc = Npcs.RANNVEIG,
    x = 2513,
    z = 3865,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rannveig
spawn_npc(
    npc = Npcs.ROOSTER_1403,
    x = 2518,
    z = 3865,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Rooster
spawn_npc(
    npc = Npcs.SKRAELING_1389,
    x = 2513,
    z = 3866,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skraeling
spawn_npc(
    npc = Npcs.SKRAELING_1391,
    x = 2515,
    z = 3863,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skraeling
spawn_npc(npc = Npcs.THORA, x = 2511, z = 3871, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Thora
spawn_npc(
    npc = Npcs.THORHILD,
    x = 2502,
    z = 3852,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Thorhild
spawn_npc(
    npc = Npcs.VALGERD,
    x = 2511,
    z = 3854,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Valgerd

package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.GLACOR,
    x = 4177,
    z = 5714,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor
spawn_npc(
    npc = Npcs.GLACOR,
    x = 4175,
    z = 5718,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor
spawn_npc(
    npc = Npcs.GLACOR,
    x = 4171,
    z = 5724,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor
spawn_npc(
    npc = Npcs.GLACOR,
    x = 4174,
    z = 5707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor
spawn_npc(
    npc = Npcs.GLACOR,
    x = 4182,
    z = 5707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor
spawn_npc(
    npc = Npcs.GLACOR,
    x = 4190,
    z = 5707,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor
spawn_npc(
    npc = Npcs.GLACOR,
    x = 4203,
    z = 5708,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor
spawn_npc(
    npc = Npcs.GLACOR,
    x = 4210,
    z = 5714,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor
spawn_npc(
    npc = Npcs.GLACOR,
    x = 4199,
    z = 5715,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor
spawn_npc(
    npc = Npcs.GLACOR,
    x = 4189,
    z = 5718,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Glacor

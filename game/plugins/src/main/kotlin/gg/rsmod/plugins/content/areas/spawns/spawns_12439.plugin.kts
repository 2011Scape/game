package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.ZOMBIE_5304,
    x = 3092,
    z = 9670,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie added by devin
spawn_npc(
    npc = Npcs.ZOMBIE_5304,
    x = 3085,
    z = 9674,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie added by devin
spawn_npc(
    npc = Npcs.ZOMBIE_5304,
    x = 3101,
    z = 9674,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Zombie added by devin
spawn_npc(
    npc = Npcs.SKELETON,
    x = 3121,
    z = 9672,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by devin
spawn_npc(
    npc = Npcs.SKELETON,
    x = 3112,
    z = 9671,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by devin
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 3123,
    z = 9665,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by devin
spawn_npc(
    npc = Npcs.SKELETON_92,
    x = 3110,
    z = 9675,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Skeleton added by devin
spawn_npc(npc = Npcs.RAT, x = 3121, z = 9664, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat added by devin
spawn_npc(npc = Npcs.RAT, x = 3104, z = 9673, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat added by devin
spawn_npc(npc = Npcs.RAT, x = 3093, z = 9671, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Rat added by devin

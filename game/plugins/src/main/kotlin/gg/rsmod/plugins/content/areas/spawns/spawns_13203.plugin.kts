package gg.rsmod.plugins.content.areas.spawns

spawn_npc(
    npc = Npcs.MALE_SLAVE_4975,
    x = 3296,
    z = 9412,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Male slave
spawn_npc(
    npc = Npcs.MALE_SLAVE_4975,
    x = 3304,
    z = 9412,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Male slave
spawn_npc(
    npc = Npcs.MALE_SLAVE_4976,
    x = 3270,
    z = 9415,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Male slave
spawn_npc(
    npc = Npcs.MALE_SLAVE_4976,
    x = 3296,
    z = 9413,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Male slave
spawn_npc(
    npc = Npcs.FEMALE_SLAVE_4977,
    x = 3305,
    z = 9411,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Female slave
spawn_npc(
    npc = Npcs.FEMALE_SLAVE_4977,
    x = 3297,
    z = 9414,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Female slave
spawn_npc(
    npc = Npcs.FEMALE_SLAVE_4978,
    x = 3295,
    z = 9412,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Female slave
spawn_npc(
    npc = Npcs.FEMALE_SLAVE_4978,
    x = 3297,
    z = 9412,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Female slave
spawn_npc(
    npc = Npcs.GUARD_4998,
    x = 3289,
    z = 9412,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Guard

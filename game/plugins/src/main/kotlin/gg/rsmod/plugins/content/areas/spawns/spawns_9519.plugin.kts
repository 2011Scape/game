package gg.rsmod.plugins.content.areas.spawns

spawn_npc(npc = Npcs.GARGH, x = 2413, z = 3053, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Gargh
spawn_npc(npc = Npcs.SCARG, x = 2380, z = 3043, height = 0, walkRadius = 5, direction = Direction.NORTH, static = false) // Scarg
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2391,
    z = 3053,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2392,
    z = 3051,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2394,
    z = 3043,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2394,
    z = 3045,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2394,
    z = 3056,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2395,
    z = 3052,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2397,
    z = 3040,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2397,
    z = 3053,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2398,
    z = 3047,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2399,
    z = 3046,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2400,
    z = 3040,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
spawn_npc(
    npc = Npcs.SWAMP_TOAD,
    x = 2402,
    z = 3042,
    height = 0,
    walkRadius = 5,
    direction = Direction.NORTH,
    static = false,
) // Swamp toad
